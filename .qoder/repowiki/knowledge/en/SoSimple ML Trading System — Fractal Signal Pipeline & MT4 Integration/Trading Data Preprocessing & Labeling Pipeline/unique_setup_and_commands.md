Activate the project venv first (`source ~/git/SoSimple/.venv/bin/activate`). Full pipeline: `python processing/label_main.py --input MT/MQL4/Files/Nero.csv [--debug|--no-normalize|--include-predict-in-front-back-pool]`. Online causal preprocessing: `from processing.online_causal_preprocessing import preprocess_online_csv`. Purge split boundaries: `python processing/purge_split.py --train ... --val ... --test ... --purge-hours 30`.