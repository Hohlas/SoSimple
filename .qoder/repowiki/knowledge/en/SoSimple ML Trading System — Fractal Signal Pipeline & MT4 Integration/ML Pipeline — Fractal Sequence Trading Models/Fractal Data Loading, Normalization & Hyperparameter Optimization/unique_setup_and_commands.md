Run hyperparameter optimization via CLI: `python -m ML.optimize --model bilstm --task classification --trials 50 --epochs 30 --seed 42`. Data cache lives under `DATA/` as `X_train.npy`, `mask_train.npy`, `y_train_<target>.npy`; pass `--clear_cache` or set `clear_cache=True` in `create_data_loaders` to regenerate. Schema validation requires `docs/schemas/fractal_v24_raw_price.schema.json` to exist.