# To Do — замечания на будущее

Найдены в ходе feature foundation rebuild (ветка `feature/limit-order-entry-convention`, 2026-06-01).

## Парсинг и тензор

- [ ] `data_loader.py:parse_fractals_to_3d()`: поля 17–20 (up_3, dn_3, up_6, dn_6) не включаются в 3D-массив X. Индексы пропущены — `if k >= 17 and k < 21: continue`. Нужно включить их наравне с полями 1–16.
- [ ] После включения 17–20 в тензор: `N_FRACTAL_FEATURES` пересчитать.

## Нормализация

- [ ] `normalize.py`: `UPDN_FIELDS` содержит только длинные горизонты (`up_12..dn_48`). После включения 17–20 в тензор — расширить до всех 10 полей.
- [ ] `normalize.py`: перейти от общего p85/p99 для всех up/dn к раздельной нормализации — каждая пара up_X/dn_X со своим p85/p99. Сейчас up_48 (~0–50) доминирует над up_3 (~0–3) в общем пуле, короткие горизонты сжимаются в околонулевой диапазон. Это же могло исказить результаты абляции — path_long/path_short выделились на фоне остальных групп из-за pooled-нормализации. После раздельной — перезапустить абляцию.

## Данные и MT4

- [x] Nero.csv перегенерирован: 23 поля, shift = `SHIFT(F[f].T) - cur_bar`.
- [ ] `delta_shift[99] = 0` — временное решение. Правильно: маска валидности для последнего фрактала (нет fractal100 для сравнения).
- [ ] `strong` — частота повышена перенастройкой параметров индикатора. После перегенерации данных — переоценить распределение strong и его вклад в engineered-признаках.

## Split

- [x] Split уже хронологический: train 2004→2019, val 2019→2022, test 2022→2026.

## Engineered-признаки

- [ ] `build_grouped_features` включает `ret_dir_atr_lag1` (unsafe, `live_safe_audit.py:84`). Исключён в `feature_ablation.py`, но не в основном коде. Нужно убрать или добавить явный фильтр.
- [ ] Абляция показала: из 9 engineered-групп только `path_long` (агрегаты up_12..dn_48, 120 признаков) и `path_short` (up_3..dn_6) показали сигнал. Результат может быть артефактом pooled-нормализации (п. Нормализация). После раздельной нормализации — перезапустить абляцию. Остальные группы (geometry, strength, break_impulse, price_position, direction, atr) пока не удалять.
- [ ] При будущих экспериментах: `reverse`, `power` не являются производными от front/back — это самостоятельные измерения MT4 (сила пробитого уровня и сумма сил фракталов на общем ценовом уровне соответственно). Не исключать их по правилу производных признаков.

## Проверка path_long

- [ ] `C_path_long_nf0` (без fractal0): PF=1.488, gate PASS. 2021: PF=949 на 17 сделках — статистический флук.
- [ ] Оба path_long-варианта дают 0 сделок в 2019 (первый год валидации). Причина: модель не находит сигнала в данных 2019 года при текущем пороге. Проверить после раздельной нормализации.

## Entry convention — исследования

- [ ] Текущий вход: pending BUY/SELL LIMIT на уровне `Close[row]`. Исследовать вариант: `Close[row] + ATR × 0.5` (ниже рынка на половину ATR). Гипотеза: увеличение буфера повысит fill_rate и может улучшить PF за счёт более выгодной цены входа.

## Нормализация цен в MT4

- [ ] `USE_NORMALIZED_OUTPUT=true` в `lib_PIC.mqh` нормализует цены фракталов в [0, 1] уже на стороне MT4. Это лишает Python возможности вычислять ATR-относительные признаки вроде `(price_i − price_0) / ATR`. Решение: отключить нормализацию цен в MT4, экспортировать сырые цены. Python сам решит, как нормализовать — min-max, z-score, ATR-relative или piecewise. MT4 — сборщик сырых данных, не нормализатор.
- [ ] Перевыполнить спецификацию `docs/superpowers/specs/2026-05-14-entry-path-all-rows-level-signal-design.md:277` (ATR-относительные признаки уровней). Сейчас реализация либо даёт мусор (`fractal_level_feature_builder.py` делит нормализованную разницу на сырой ATR), либо требует костыль с внешним OHLC-файлом (`prepare_raw_features.py`). После перехода на сырые цены в MT4: реализовать `raw_distance_atr`, `abs_distance_atr`, `directed_distance_atr` честно, без внешних файлов.

## Разметка

- [x] `label_trade_targets()`: исправлен entry price с Close текущего бара на Open следующего (`entry_open = opens[ohlc_idx + 1]`). Close — look-ahead, реальный вход возможен только на Open t+1. Требуется переразметка CSV для актуальности старых экспериментов entry_path.

## Модель — гиперпараметры

- [ ] Текущие параметры RF (`n_estimators=100, max_depth=10, random_state=42`) не оптимизированы — взяты как разумное умолчание для абляции. Оптимизировать после финального выбора признаков: GridSearch по `n_estimators` {100, 200, 300}, `max_depth` {10, 20, 30, None}, `min_samples_leaf` {1, 5, 10}. Метрика — validation PF.
- [ ] `n_estimators=100` с 200–600 признаками: количество деревьев не обязано расти с числом признаков — RF отбирает `sqrt(n_features)` на сплит (~14–25 признаков). Важнее сходимость OOB-ошибки: 100 деревьев обычно достаточно, но проверить.
- [ ] `max_depth=10` с 200+ признаками: дерево использует ≤10 признаков в любом пути — ограничивает взаимодействия. Проверить OOB при `max_depth=None` с `min_samples_leaf=5`.

## Эксперименты — направления + up/dn

- [ ] Повторить результат старого baseline: честно подать только направления f{0..99}_dir (100 признаков ±1). Подтвердить PF~1.5 на чистых направлениях без подмены под видом цен.
- [ ] К направлениям добавить up_3..dn_48 (как flat-признаки, не агрегаты). Проверить, растёт ли PF. Если нет — варьировать `max_depth`.

## EDA и проверка данных

- [ ] Усилить EDA в методике: проверять не только NaN и диапазон, но и распределение каждого поля против его семантики. Баг с подменой цены на направление не был обнаружен, потому что нормализованные цены [0, 1] и направления {-1, 1} внешне похожи. Правило: для каждого признака сверить unique-значения/гистограмму с ожидаемым типом (непрерывное vs бинарное). Если цена имеет 2 уникальных значения — ошибка.
