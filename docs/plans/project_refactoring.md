## Контекст
- Структура проекта изложена в MODULE_INDEX.md, docs/DATA_FLOW.md и AGENTS.md. Отработка этапов фиксируется в CHANGELOG.md.
- Проект состоит из множества этапов, большинство завершены. Информация о завершённых этапах, не имеющая отношения к текущей задаче, занимает контекст ИИ-агентов.
- В каталоге statistics содержатся и скрипты ранних этапов (statistics.py) и скрипт из последнего этапа (signal_tracer.py).
- В корне каталога ML — множество скриптов разных этапов.

## Цель
Структурировать проект и документацию, чтобы упростить навигацию для ИИ-агентов и разработчика.
Уменьшить объём документации и кода, загружаемого в контекстное окно при работе с конкретным этапом/задачей.

## Принципы (для агента-исполнителя)
- Решения — прагматичные, на основе best practice.
- Не домысливать. Если неоднозначно — спросить.
- При наличии альтернатив — указать плюсы/минусы.

---

## План рефакторинга

### 0. ML/: README.md вместо перемещения файлов
**Что:** Создать `ML/README.md` с таблицей файлов, разбитой по логическим слоям.
**Почему НЕ перемещать файлы:** все 18 .py файлов связаны абсолютными импортами (`from ML.train import ...`, `from ML.models import ...`). Перемещение в подкаталоги потребует обновить ~20 импортов. Нумерованные подкаталоги (01_training/) невозможны — Python не позволяет имена модулей, начинающиеся с цифры.
**Что должно быть в ML/README.md:**
- Таблица файлов по слоям (Core, Training, Evaluation) с описанием, входами/выходами, статусом
- Ссылка на docs/ML/ для архитектурной документации
- Примеры команд запуска

Подкаталоги baseline/, conformal/, models/ — уже корректно выделены, их не трогать.

### 1. README.md в каждом каталоге со скриптами
**Что:** Создать README.md в: `processing/`, `statistics/`, `API/`, `ML/` (п.0), `MT/MQL4/`.
**Содержание:**
- Список скриптов с описанием (назначение / входы / выходы / запуск)
- Примеры команд
- Статус каждого файла (флаг из единой легенды)
**Правило:** В каталоге с кодом — только README.md. Вся остальная документация — в docs/.

### 2. Переименовать каталоги docs/ для соответствия каталогам кода
**Что:**
- `docs/data_analysis/` → `docs/statistics/` (соответствует `statistics/`)
- `docs/data_preprocessing/` → `docs/processing/` (соответствует `processing/`)
- `docs/ml/` → `docs/ML/` (соответствует `ML/`)
- `docs/mql4/` → `docs/MT/` (соответствует `MT/`)
**Зачем:** Агент находит документацию по имени каталога кода без дополнительной навигации.
**Обязательно:** Обновить ВСЕ ссылки в: AGENTS.md, MODULE_INDEX.md, docs/DATA_FLOW.md, CHANGELOG.md, все .md файлы внутри docs/.

### 3. Перенести документацию на своё место
**Что:**
- `ML/implementation_plan.md` → `docs/plans/ml_implementation_plan.md`
- Если в других каталогах с кодом есть .md файлы (кроме README.md) — переместить в docs/
**Зачем:** Одно правило: в коде — README.md, документация — в docs/.

### 4. ~~STAGES.md~~ → ✅ Выполнено: навигационная таблица в DATA_FLOW.md
Вместо отдельного файла добавлена таблица "📋 Навигация по этапам" в начало docs/DATA_FLOW.md с колонками: #, Этап, Код, Docs, Статус.

### 5. Флаги статусов — единая легенда, реальные значения
**Что:** Использовать единую легенду во всех документах (AGENTS.md, MODULE_INDEX.md, README.md, DATA_FLOW.md):
✅ Активный — читай, можешь менять
🚧 В разработке
🏁 Завершён — стабилен, не меняй без причины
📦 Архив — смотри только если явно попросили
⚠️ Требует внимания

**Обязательно:** Пересмотреть статусы в MODULE_INDEX.md. Сейчас все модули ✅ — это неверно. Например: `baseline/` → 🏁, `statistics.py` → 🏁, `signal_tracer.py` → ✅.

### 6. Обновить AGENTS.md — структура проекта
**Что:** После выполнения пп. 0–5 обновить секцию "📂 Структура проекта" в AGENTS.md: новые пути docs/, флаги, ссылки на README.md и STAGES.md.

---

## Порядок выполнения
1. ✅ п.3 — перенесены ML/implementation_plan.md → docs/plans/, ML/reports/walkthrough.md → docs/ML/
2. ✅ п.2 — переименованы docs/{data_analysis→statistics, data_preprocessing→processing, ml→ML, mql4→MT}, обновлены все ссылки
3. ✅ п.0 + п.1 — созданы README.md в: ML/, processing/, statistics/, API/
4. ✅ п.4 — навигационная таблица в DATA_FLOW.md (вместо отдельного STAGES.md)
5. ✅ п.5 — пересмотрены статусы в MODULE_INDEX.md, обновлена легенда
6. ✅ п.6 — обновлён AGENTS.md: структура с флагами, таблица статусов, навигация

Каждый шаг — отдельный коммит.

