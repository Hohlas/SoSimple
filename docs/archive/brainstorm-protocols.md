# Протоколы споров «автор × критик» (idea-check)

Аудит вердиктов без чтения чата. 19 кластеров, все споры состоялись.

## Volatility regime transition prediction [кластер 1]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Идей в кластере: Volatility regime transition prediction; Time-to-event survival modeling; Multi-scale amplitude decomposition; Hawkes Volatility Clustering; Волатильность как аномалия
- Блок идеи:
```
Название: Volatility regime transition prediction
Суть: Predict transitions between volatility regimes (low→high, high→low) rather than price direction. Train on realized volatility clustering and regime change points. Trade when regime transition probability exceeds threshold, with direction-agnostic straddle-like positions.
Обходимый тупик: already-moved, regime drift
Откуда edge: [гипотеза] Volatility clustering is well-documented in financial markets; large moves tend to follow large moves. The edge comes from predicting when volatility will expand, not which direction price will move.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Amplitude Regime / движение-фильтр (секция 2.11): предсказание амплитудно-волатильного режима без направления цены — тот же механизм edge (структура/расширение волатильности, не знак)","retrospective_ref":"Секция 2.11: «07-07 Amplitude Regime — режим объясняется временем+ATR»; итог 2.11: «движение-фильтр — единственный стабильный результат; сигнал — почти всё „уже случившееся движение"; направление отклонено»; секция 2.1: «MI(amplitude) 0.010–0.022 против MI(direction) 0.003–0.004 bits», «реальный потолок — амплитуда, не знак»; секция 3: амплитудно-движенческий режим «заморожен как research-only», то есть уже проверен и не прошёл торговый порог"}, "vulnerabilities":["Происхождение edge — автокорреляция ATR, а не предсказуемый переход: волатильная кластеризация означает, что лучший предиктор будущего ATR — сам ATR (AR-1), а часовая компонента режима уже поймана календарём; ретроспектива 2.11 прямо утверждает «режим объясняется временем+ATR», а 2.12 фиксирует календарную доминантность (no-ML hour-baseline ≥85.9% ML PF). Заявленное «откуда edge» (volatility clustering) — это то же, что рынок уже учел в спреде и цене опционов; MI(amplitude) 0.010–0.022 bits — потолок, не торгуемая премия.","Заявленный обход тупика already-moved ложный: по 2.11 «сигнал — почти всё уже случившееся движение» — момент подтверждения перехода режима и есть момент, когда расширение уже произошло (Spearman с будущим движением ≈0). Straddle-подобная позиция в spot-FX платит двойной спред на входе (обе ноги) и гарантированный SL одной ноги при любой реализации, а спред 0.8 уничтожает большинство кандидатов (2.12); в момент, когда волатильность реально расширяется, спред брокера расширяется синхронно, съедая edge именно там, где он должен платить; плюс requote ERROR-138 (2.5). Направленческий риск не устранён: без прогноза знака PnL зависит от того, что |движение| превысит двойной спред, — это та же ставка на амплитуду, которая оказалась already-moved.","Малый N и нестационарность самих переходов: переходы low→high — редкие события (десятки сделок/год), bootstrap CI широкие, кандидатские N в десятках уже были причиной откатов (2.2, 2.4); частота переходов режима сама дрейфует — перелом на границе 2023 устойчиво проваливает модели 2004–2022 на 2023–2026 (2.8), то есть «режимный дрейф», который идея называет обходимым, является прямой причиной нестабильности предсказания переходов, а не отвлекающим фактором."],"falsification":{"test":"Убивающий тест: на замороженном out-of-sample с live-safe хронологией fill (контракт из 2.12) обучить классификатор перехода режима low→high на реализованной волатильности и сравнить прогноз будущего расширения log(realized vol) с тривиальным бенчмарком AR(1)-на-log(ATR) + час дня. Затем смоделировать вход только при прогнозе перехода (порог вероятности) с двусторонним спредом и requote-моделью, против контроля «всегда в рынке на расширение после подтверждённого перехода» (already-moved baseline).","metric":"Инкрементальный R² прогноза расширения поверх baseline AR(1)+календарь; net PF на frozen OOS с двусторонним спредом и requote; bootstrap CI нижней границы PF","kill_threshold":"Идея убита, если инкрементальный R² прогноза расширения над AR(1)-ATR+час ≤ 0.005, ЛИБО net PF на frozen OOS (спред x2, requote) < 1.3, ЛИБО нижняя граница bootstrap CI ≤ 1.0; дополнительный контроль: доля уже состоявшегося движения на момент сигнала ≥ 50% (воспроизведение тупика already-moved из 2.11)","cost":"3-5 дней (инфраструктура амплитуды/регрессии из 2.11 и цикл frozen-оценки уже существуют; новый только классификатор переходов и straddle-симулятор)","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["novelty: дубликат Amplitude Regime/движение-фильтр (2.11) — тот же механизм edge (амплитуда без знака), уже заморожен research-only","источник edge: автокорреляция ATR, уже учтена календарём и самим ATR (2.11 «режим объясняется временем+ATR», 2.12 no-ML hour-baseline ≥85.9% ML PF)","обход already-moved ложный: момент подтверждения перехода = уже состоявшееся расширение, Spearman с будущим ≈0 (2.11)","исполнение: двойной спред straddle при spread 0.8 (2.12) и requote ERROR-138 (2.5)"],"defense":"Признаю поражение по существу; цитаты проверил — они точны. Моё «откуда edge» (volatility clustering) — это и есть автокорреляция ATR, которую ретроспектива уже разложила на календарь и сам ATR: предсказание перехода сводится к AR(1), инкрементального знания нет. Обход already-moved несостоятелен: подтверждение перехода приходит после расширения, торгуется «после факта» — тот самый тупик 2.11, который идея обещала обойти. Механика «кто платит» тоже фатальна: straddle платит двойной спред на входе, а спред расширяется синхронно с волатильностью именно там, где должен платить edge; при spread 0.8 (PF 1.32) и requote кандидат не проходит. Малый N переходов и дрейф их частоты (перелом 2023, 2.8) — не отвлекающий фактор, а прямое опровержение заявленного обхода regime drift. Убивающий тест против AR(1)+час и already-moved-контроля корректен и обязателен. Идея — переупаковка замороженного амплитудного режима; убита честно."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Survival Competing Risks Exit [кластер 2]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Идей в кластере: Survival Competing Risks Exit; Survival exit timing; MFE conditional quantile exit
- Блок идеи:
```
Название: Survival Competing Risks Exit
Суть: Модель выживаемости с конкурирующими рисками: вместо тройного барьера, моделировать hazard-функции для TP/SL/timeout как конкурирующих событий. Оптимизировать hold duration по conditional survival probability, а не по фиксированным барьерам.
Обходимый тупик: не указан
Откуда edge: [гипотеза] конкурирующие риски — стандарт в medical statistics; hazard-модель учитывает time-varying covariates и цензурирование, что даёт более точный exit timing чем фиксированные пороги.
Теги: похоже на: тройной барьер
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Механизм «оптимизация времени/порога выхода без предсказания направления» уже исчерпан и закрыт: (а) тройной барьер 2.3/4 — его структура TP/SL/timeout и есть конкурирующие события, идея лишь меняет оценщик (hazard-модель) на тех же исходах; (б) 2.6 — «абсолютный probability-threshold мёртв», т.е. порог по вероятности исхода (а conditional survival probability — это именно порог по вероятности) уже отвергнут; (в) 2.4 — quantile exit и track-a-max-out REJECT, нестабилен по сидам; (г) 2.14 — R-multiple exit дал 0 стабильных правил.","retrospective_ref":"Секция 4, «Что не работает», буллет «Тройной барьер как торговый слой (см. 2.3) — label-баги искажали PF, после фикса слой не превосходит baseline; проблема не решается поиском exit-порогов»; секция 2.3 «Итог: TB перспективен на validation, разваливается на тесте; слой не превосходит baseline»; секция 2.6 «абсолютный probability-threshold мёртв, работает только top-k»; секция 2.4 «track-a-max-out REJECT (лучший val PF 0.478)»"},"vulnerabilities":["Нет нового источника edge, только переоценка известного: hazard-модель перераспределяет вероятности между теми же исходами TB (TP/SL/timeout) и не добавляет информации о будущем движении. Ретроспектива фиксирует отсутствие знака (2.1 MI direction FAIL, потолок R² 0.006–0.030; 2.11 already-moved) и календарную доминантность (2.12: no-ML hour-baseline ≥85.9% от ML PF) — survival-кривые будут реконструировать тот же календарно-амплитудный режим, из которого фиксированные барьеры уже извлекли PF≈1.0–1.28 и не больше."],"falsification":{"test":"Обучить survival-модель с конкурирующими рисками (Cox/Aalen с time-varying covariates) на live-safe признаках из 2.5, exit по порогу conditional survival probability; оценить на frozen-тесте 2023–2026 с корректной хронологией fill (контракт 2.12) против fixed-barrier baseline TB из 2.3 как контроль.","metric":"PF на OOS со спредом и проскальзыванием + bootstrap CI (нижняя граница); дополнительно дельта PF относительно fixed-barrier TB baseline.","kill_threshold":"Идея убита, если нижняя граница bootstrap CI ≤ 1.0, ИЛИ прирост PF над фиксированными барьерами TB статистически неотличим от нуля (например, ΔPF < 0.05) — это прямо воспроизводит вывод 2.3 «проблема не решается поиском exit-порогов». Учитывая, что порог успеха проекта PF≥1.3 с CI>1.0, а исторически все эквивалентные постановки дали PF≤1.28 или развалились на тесте, ожидаемый исход — отказ.","cost":"2–4 дня (данные, live-safe контур и симулятор готовы из 2.5/2.12; ограничение — CPU-only из 2.5, обучение survival-моделей быстрое).","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession": true, "accepted_arguments": ["novelty(a): тройка исходов TP/SL/timeout уже есть конкурирующие риски — меняется лишь оценщик, не механизм", "novelty(b): порог по conditional survival probability — это абсолютный probability-threshold, мёртвый по 2.6", "vulnerabilities: заявленный edge — моделирование, а не информация; без знака (MI direction FAIL, R²-потолок 0.006–0.030) hazard TP vs SL не даст больше фиксированных барьеров"], "defense": "Прямая цитата секции 4 «проблема не решается поиском exit-порогов» бьёт точно в этот механизм — признаю фатальной. Секция 2.6 подтверждает: работает только top-k, а не пороги по вероятности. Динамические time-varying ковариаты могли бы дать адаптивный выход, но 2.8 показал судьбу динамического trailing-выхода: PF 1.831 на 2019–2022 → PF 0.897 на 2023–2026, permutation p=1.000. Цензурирование и hazard-оценка — честные статистические уточнения, но они перераспределяют вероятности между теми же исходами, не добавляя информации о будущем движении; календарная доминантность (2.12) означает, что survival-кривые реконструируют тот же амплитудный режим с уже извлечённым PF≤1.28. Тест критика корректен по дизайну и kill-threshold, но ожидаемый исход — отказ. Идею считаю убитой; отдельное признание: мой аргумент «standard in medical statistics» — перенос метода, а не источник edge."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Microstructure imbalance prediction [кластер 3]

- Статус: полный спор (3 раунда)
- Идей в кластере: Microstructure imbalance prediction; Queue Imbalance Microstructure; Nero tick-flow imbalance
- Блок идеи:
```
Название: Microstructure imbalance prediction
Суть: Predict short-term order flow imbalances using tick-level data (if available) or high-frequency proxies (bid-ask spread changes, intra-bar price distribution). Target is microstructure imbalance, not directional move.
Обходимый тупик: already-moved
Откуда edge: [гипотеза] Order flow has short-term predictability due to institutional execution algorithms and hedging flows; microstructure signals decay fast but may be exploitable at appropriate frequency.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":null,"retrospective_ref":null},"vulnerabilities":["Данные/источник edge отсутствует в проекте. Проект имеет только одноисточниковый OHLC (секция 7: «одноисточниковый OHLC меняет результаты»; MT4/MT5 дают разные события); тиковых данных нет — оговорка «if available» в идее это маскирует. Прокси-ветка (спред-изменения, внутрибаровая дистрибуция) — это уже протестированные structure-фичи: секция 2.9 «структура — главный носитель (no_structure рушит AUC до 0.534/0.500)», но в 2.8 breach конвертируется только в +0.20 PF («силы на спред не хватает»), 2.10 price action/геометрия — TRADING_GATE_FAILED. Ближайший in-repo прецедент кластера — 2.13 Nero: «Batch Selection BATCH_NO_WINNER — 11 eligible из 32, все провалили BS_p05>1.0, топ PF 1.232 (gross)»; содержимое сигналов Nero в ретроспективе не описано, поэтому это не цитатный дубль механизма, но эмпирический удар по нему.","«Imbalance ≠ направление» — иллюзия при торговле: прибыльная ставка на imbalance — это ставка на знак следующего движения, а сторона знака закрыта априорно. Секция 2.1: «direction FAIL (val p=0.229)», «MI(direction) 0.003–0.004 bits», «сторона знака движения закрыта априорно» (секция 7). Секция 2.11: «корреляция с движением от next-open ≈0 (H12 0.001)», «Spearman из fractal0 0.8786, с будущим движением ≈0 (−0.0149/−0.0336)», «на H3 у 57% строк ≥50% движения уже состоялось». Структурные прокси из OHLC предсказывают уже состоявшееся движение (already-moved — тот самый тупик, который идея декларирует как обходимый), а не латентное давление. Пока не появится реально новый тиковый источник, механизм edge не имеет носителя: дисбаланс-прокси из OHLC несут ту же информацию, что уже проваленные фичи.","Исполнение и стоимость на требуемой частоте. Микроструктурные сигналы «гниют быстро» — нужен M1/M5/тик, а проект на коротком горизонте системно проигрывает: секция 2.12 «spread 0.2→PF 3.27, 0.4→2.72, 0.8→1.32» (M5-winner деградирует до 1.32 при спреде 0.8); «после фикса хронологии fill PnL → −530.51, PF 2.82–3.42→0.82–0.94, edge исчез» — внутрибаровая хронология fill не решена и критична именно для коротких горизонтов (секция 6/7: «от неё зависит весь edge»); секция 2.5 «requote ERROR-138, 8/87 входов не открылись»; секция 2.12 «no-ML hour-baseline ≥85.9% ML PF — календарная доминантность» — на HF календарная составляющая доминирует ещё сильнее. Суммарно: тройной барьер (спред + хронология + requote + календарь) съедает микроструктурную премию до её появления."],"falsification":{"test":"На существующих OHLC-прокси (внутрибаровая позиция close в диапазоне, ratio/структурные фичи, нормализованные ATR — те же, что в 2.9/2.11) проверить направленческую информацию двумя шагами: (a) Spearman прокси → знаковое движение next-open→close+H3 по live-safe источникам (контракт хронологии fill из 2.12, без future-полей из 2.5); (b) если (a) прошло — торговый гейт: top/bottom-децили прокси, вход next-open/limit, спред 0.8, bootstrap CI.","metric":"(a) |Spearman ρ| прокси vs будущее знаковое движение, и MI(прокси; направление H3) в битах; (b) OOS PF с bootstrap CI (нижняя граница).","kill_threshold":"(a) |ρ| ≤ 0.02 или MI ≤ 0.010 bits — зеркало результатов 2.11 (0.001/0.0336) и 2.1 (0.003–0.004): механизм мёртв, тиковый источник не создаёт новой информации в доступных данных; (b) OOS PF < 1.3 с нижней границей CI ≤ 1.0 — порог проекта.","cost":"дни (2–4): прокси-инженерия + оценка на существующем пайплайне; тиковая ветка — недели–месяцы сбора данных, в текущих данных проекта недоступна","falsifiable":true},"preliminary_verdict":"условно"}
```

### Раунд 2 — ответ автора
```json
{"concession": true, "accepted_arguments": ["Аргумент 2 фатален: ставка на imbalance — ставка на знак, а знак закрыт априорно (2.1, 2.11); OHLC-прокси несут информацию уже состоявшегося движения", "Аргумент 1: тиковых данных нет, «if available» маскирует отсутствие источника; прецедент Nero (2.13) — BATCH_NO_WINNER, все 11 eligible провалили BS_p05, топ PF 1.232 (gross)", "Аргумент 3: тройной барьер — спред 0.8 рушит PF до 1.32, хронология fill не решена (2.12), requote 8/87 (2.5)"], "defense": "Проверил цитаты в retrospective.md — все точны, признаю поражение по существу. Прокси-ветка (внутрибаровая позиция, ratio, структура) — те же фичи, что в 2.9/2.11: они предсказывают уже состоявшееся движение, а прибыльная ставка на imbalance эквивалентна ставке на знак, который закрыт априорно. Механизм edge носителя не имеет; [гипотеза] о латентном давлении институционального потока остаётся непроверяемой без реального тикового источника, которого в проекте нет — это закупка данных, а не гипотеза. Дополнительно признаю слабость механизма «кто платит»: конкретный контрагент, платящий за премию, не идентифицирован. Даже при гипотетическом источнике тройной барьер исполнения съедает микроструктурную премию на требуемой частоте. Идею отзываю."}
```

### Раунд 3 — итог критика
```json
{"name": "Microstructure imbalance prediction", "pros": ["Автор содержательно защищал идею: проверил все цитаты, не оспорил ни один факт и сам отозвал идею — спора по фактам нет", "Идея честно помечена как [гипотеза] и «радикальная» и не притязает на направленный прогноз, корректно ограничившись оговоркой «if available»"], "cons": ["Ставка на imbalance = ставка на знак, а знак закрыт априорно (2.1 MI direction FAIL, 2.11 already-moved) — поражено ядро идеи, а не параметр", "Тиковых данных нет, «if available» маскирует отсутствие источника; прокси-ветка — те же structure-фичи 2.9/2.11, предсказывающие уже состоявшееся движение", "Прецедент Nero BATCH_NO_WINNER (2.13): попытка вытащить edge из одноисточникового OHLC уже провалилась", "Тройной барьер исполнения на требуемой частоте: спред 0.8→PF 1.32, хронология fill не решена (2.12), requote (2.5) — съедает премию даже при гипотетическом источнике", "Механизм «кто платит» не идентифицирован: контрагент институциональных алго-потоков не назван, носитель edge отсутствует", "Гипотеза непроверяема без закупки тиков — это закупка данных, а не исследовательская гипотеза; автор сам это признал"], "novelty": {"duplicate_of": null, "retrospective_ref": ["2.9", "2.11", "2.13"]}, "vulnerabilities": ["Imbalance ≠ направление — иллюзия: прибыльная ставка на imbalance сводится к ставке на знак, знак закрыт априорно (2.1, 2.11)", "Отсутствие данных и источника edge: тиков нет, прокси-ветка = already-moved (2.9/2.11), прецедент Nero (2.13)", "Тройной барьер исполнения/стоимости на HF: спред 0.8→PF 1.32, хронология fill (2.12), requote (2.5)", "Отсутствие механизма «кто платит»: edge без носителя, гипотеза непроверяема без закупки тиков"], "falsification": {"condition": "Единственная тестируемая ветка — прокси-баланс — уже фальсифицирована: фичи 2.9/2.11 предсказывают уже состоявшееся движение, прецедент Nero BATCH_NO_WINNER (2.13)", "untestable_branch": "Тиковая ветка непроверяема: источника данных нет, закупка тиков — закупка данных, а не гипотеза", "new_data_from_defense": false}, "potential": 1, "potential_why": "Механизм «кто платит» не идентифицирован: автор подтвердил отсутствие носителя edge, контрагент институциональных потоков не назван, а единственный тестируемый вариант уже фальсифицирован.", "verdict": "убита", "rationale": "Защита содержательна и совпадает с атакой: автор по существу подтвердил, что прокси = already-moved (2.9/2.11), механизм «кто платит» отсутствует, гипотеза непроверяема без закупки тиков, а тройной барьер исполнения съедает премию даже при гипотетическом источнике — признанный аргумент фатален для ядра идеи, поэтому вердикт меняется на «убита» по существу, а не из-за уступчивого тона."}
```

## Flow Toxicity VPIN Regime [кластер 4]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Идей в кластере: Flow Toxicity VPIN Regime; Adverse selection cost model as signal
- Блок идеи:
```
Название: Flow Toxicity VPIN Regime
Суть: Вычислять VPIN (Volume-Synchronized Probability of Informed Trading) по тиковому потоку как индикатор токсичности потока. Торговать только в режимах низкой токсичности, когда спред стабилен и исполнение предсказуемо. Не предсказывает направление — фильтрует моменты, когда рынок «нормален».
Обходимый тупик: календарная доминантность
Откуда edge: [гипотеза] информированные трейдеры создают кластеры токсичного потока; в тихие периоды spread-составляющая ниже, а fill-качество выше — это механика исполнения, а не прогноз цены.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Секция 2.13, Execution Hygiene: механизм «качество исполнения (fill) определяет PF» — тот же механизм edge, что заявлен в идее («fill-качество выше — это механика исполнения, а не прогноз цены»), уже протестирован и отвергнут","retrospective_ref":"Секция 2.13: «08-01 Execution Hygiene — OPEN_FAILED=22767 (повторы политики), fill rate↔PF Spearman −0.46, гипотеза „плохой fill брокера" отвергнута»; дополнение — секция 2.2: «„устойчивый PF фильтров" не утверждён — прирост всего +6.6 пп, просадка по годам, кандидатские N — десятки» (фильтрация моментов без прогноза направления как класс также не дала устойчивого edge)"},"vulnerabilities":["Данные: VPIN физически невычислим. В проекте нет ни тикового, ни объёмного потока (Nero.csv — только fractal-признаки, spread-телеметрия — один день 2026.07.28, min/max 7–56 пунктов). VPIN по H1/M5 OHLC без объёма — не VPIN, а шумный прокси режима, который ретроспектива уже свела к календарю и ATR: «07-07 Amplitude Regime — режим объясняется временем+ATR» (2.11). Более того, заявленный канал edge — «исполнение» — это самое слабое звено проекта: фикс хронологии fill (07-29) уничтожил весь edge locked-test (2.12), спред 0.8 убивает большинство кандидатов (2.12, секция 7). Измерять «fill-механику» в симуляторе, чья модель исполнения только что признана невалидной, — circular.","Стационарность/календарная доминантность: режим «низкой токсичности» по построению коррелирует с часами низкой активности (ночь/азиатская сессия). Ретроспектива: «no-ML hour-baseline ≥85.9% ML PF (медиана 0.98) — календарная доминантность; leaderboard 0/11 multi-seed, все winner-правила time_only/movement_plus_time» (2.12). Тупик, который идея заявляет обойти, не обходится: любой VPIN-фильтр сожмётся в hour-фильтр, а hour-фильтры edge не создают (они лишь объясняют уже видимый PF). VPIN имеет собственную внутридневную сезонность по конструкции — информация фильтра в значительной мере календарная.","Происхождение edge противоречит собственным находкам проекта: (1) гипотеза «качество исполнения → PF» уже отвергнута: «fill rate↔PF Spearman −0.46» (2.13) — связь отрицательная, т.е. лучший fill не даёт лучший PF; (2) предпосылка «в тихие периоды спред ниже» не проверена и опровергается телеметрией: в тихие ночные часы спред XAUUSD на единственном измеренном дне достигал 50+ пунктов (spread_XAUUSD_stats.csv) — в отсутствие ликвидности спред расширяется, а adverse selection остаётся; (3) единственный стабильный сигнал проекта — амплитудный movement-фильтр (2.11) — заморожен как research-only, и он отбирает БОЛЬШИЕ движения, т.е. противоположный режим; не-направленный «исполнительный» edge не имеет ни одного прецедента успеха за 6 месяцев."],"falsification":{"test":"Фиксированная механическая стратегия (заранее предвыбранная, напр. baseline A @ 7.5% из 2.4, либо movement-фильтр 2.11) исполняется ТОЛЬКО в окнах «низкой токсичности» (прокси по нижнему терцилю volume-прокси/спреда/ATR на OHLC; при сборе тиков — реальный VPIN) на frozen split с canonical spread. Обязательные контроли: (a) календарно-матчед окна — те же часы/дни недели, НЕ обусловленные токсичностью; (b) permutation-тест — перестановка меток токсичности во времени; (c) spread stress 0.4 и 0.8.","metric":"OOS PF с canonical spread; bootstrap CI нижняя граница; инкремент PF(low_tox) − PF(calendar-matched); n сделок в режиме; permutation p-value; PF при spread stress","kill_threshold":"Идея убита, если: инкремент над календарно-матчед контролем ≤ +0.10 (VPIN не добавляет ничего сверх часа), ИЛИ нижняя граница bootstrap CI ≤ 1.0, ИЛИ n сделок < 100 (широкий CI, десятки сделок — паттерн всех провалов 2.2/2.4), ИЛИ PF < 1.3 при spread 0.4–0.8 (порог успеха проекта не достигнут после честных издержек)","cost":"3–5 дней на OHLC-прокси с существующими данными; недели (и сбор тикового/объёмного потока на весь бэктест-период 2004–2026, который в проекте отсутствует) — если требовать настоящий VPIN","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["Канал edge «качество исполнения» отвергнут точной цитатой из 2.13: fill rate↔PF Spearman −0.46 — связь отрицательная, лучший fill не даёт лучший PF; идея заявляла именно этот механизм","Предпосылка «в тихие периоды спред ниже» опровергнута единственной телеметрией spread_XAUUSD_stats.csv: в тихие часы 2026.07.28 спред достигал 50–56 пунктов — adverse selection остаётся при расширенном спреде","VPIN физически невычислим: Nero.csv содержит только fractal-признаки и ATR, ни тикового, ни объёмного потока нет"],"defense":"Признаю поражение по существу, а не формально. (1) Заявленный канал edge — fill-механика — это ровно гипотеза 2.13, отвергнутая с отрицательной корреляцией; переформулировка не спасает, т.к. «исполнительный» не-направленный edge не имеет ни одного прецедента за 6 месяцев. (2) VPIN как заявлено невозможен без тиков/объёма; единственная реализация — OHLC-прокси, который ретроспектива уже свела к времени+ATR (2.11) и hour-baseline ≥85.9% (2.12) — это и есть тупик, который идея обходит только на словах: любой прокси токсичности сожмётся в hour-фильтр. (3) Предпосылка «тихо=дешево» эмпирически ложна по телеметрии спреда. Ценность сохраняет только протокол фальсификации (календарно-матчед контроль, permutation, spread stress 0.4/0.8) — как готовый инструмент проверки любых режимных фильтров, включая amplitude-режим 2.11. Но как гипотеза идея мертва: механизм отвергнут, данные отсутствуют, prior — провал."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Regime transition early warning system [кластер 5]

- Статус: полный спор (3 раунда)
- Идей в кластере: Regime transition early warning system; BOCPD Regime Switching; Adversarial regime gate
- Блок идеи:
```
Название: Regime transition early warning system
Суть: Model regime transitions themselves (2023 breakpoint killed strategies in 2.8). Target is probability of regime change within N bars. Use change-point detection, structural break tests. Reduce position size or exit when regime change likely.
Обходимый тупик: regime drift
Откуда edge: [гипотеза] Regime transitions have precursors (volatility increase, correlation breakdown, volume pattern changes); detecting these allows defensive positioning before strategy breakdown.
Теги: 
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":null,"retrospective_ref":null},"vulnerabilities":["Происхождение edge: допущение о предвестниках смены режима противоречит накопленным фактам. Перелом 2023 резкий и устойчив к расширению обучения (секция 2.8: 'walk-forward — расширение обучения не помогает, провал 2023–2026 устойчив') — если бы предвестники были в признаках, модель, обученная до 2022, их бы частично поймала. Сам режим объясняется тривиально: '07-07 Amplitude Regime — режим объясняется временем+ATR' (2.11), а MI по будущему движению ≈ 0, direction FAIL (2.1). Иными словами, сигнал 'режим сменится через N баров' — это прогноз будущего движения, который проект многократно и независимо провалил; свободного места под экзотические предвестники (correlation breakdown, volume) в данных не видно.","Размер выборки: значимых смен режима в ряде 2004–2026 — единицы (по ретроспективе релевантен один перелом на границе 2023). Классификатор 'перелом в следующие N баров' с N=1–3 позитивными примерами необучаем и невалидируем: bootstrap CI бессмысленен, порог PF ≥ 1.3 с нижней границей CI > 1.0 априори недостижим. Защитный фильтр вдобавок порежет сделки до десятков — паттерн, уже убивавший кандидатов (2.2: 'кандидатские N — десятки'; 2.4: median 8 сделок).","Внутренняя противоречивость механики: change-point detection и structural break тесты (CUSUM, Chow, BOCPD) по своей природе ретроспективны — подтверждают перелом ПОСЛЕ накопления данных о нём. Edge идеи требует опережения ('early warning', уменьшение позиции ДО развала стратегии). Следовательно, работающий детектор — это прогноз перелома, а не детекция, а прогноз в этом проекте не подтвердился ни разу. Дополнительный риск: честный causal-контур обязателен, иначе детектор повторит судьбу future-derived систем (2.5: 'все исторические системы FAIL')."]}
"falsification":{"test":"На зафиксированной хронологии (locked-данные 2022-12..2026-06 + исторический ряд) определить точки смены режима (CUSUM/Chow на ATR, p<0.05, минимальный интервал между переломами), посчитать их число. Затем классификатор 'смена режима в следующие N баров' на causal-окне без future-признаков (по контракту live-safe из 2.5) против базлайна время+ATR (по правилу календарной доминантности из 2.12 и 'режим объясняется временем+ATR' из 2.11); плюс измерить лаг подтверждения детектора относительно фактического перелома.","metric":"N детектируемых переломов в 2004–2026; ΔAUC(опережающий классификатор − время+ATR базлайн) с пермутационным p; лаг подтверждения в барах.","kill_threshold":"Идея убита, если: N < 5 переломов (задача необучаема), ИЛИ ΔAUC < 0.05 при perm p ≥ 0.05 (предвестники не превосходят тривиальный базлайн), ИЛИ детектор подтверждает перелом с лагом ≥ 1 бар (нет опережения — 'early warning' не существует, защитный фильтр опоздал бы и не дал бы uplift).","cost":"1–3 дня (данные, locked-тест, пермутационная и MI-инфраструктура уже готовы; нужны только causal-окно и сравнение с базлайном)","falsifiable":true},"preliminary_verdict":"условно"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["Размер выборки (vuln 2): в ряде 2004–2026 значим один перелом на границе 2023 ('перелом режима на границе 2023', хронология июня); классификатор с 1–3 позитивными примерами необучаем и невалидируем — неустранимо.","Внутренняя противоречивость (vuln 3): CUSUM/Chow/BOCPD подтверждают перелом после накопления данных; лаг ≥ 1 бар — свойство класса методов, а не реализации; kill-критерий 'лаг < 1 бар' априори недостижим без прогноза, а прогноз в проекте не подтверждался ни разу — неустранимо.","Происхождение edge (vuln 1): фактам противоречит — расширение обучения не помогает (2.8, цитата верна), режим объясняется временем+ATR (2.11), корреляция с будущим движением ≈ 0 (2.11); предвестники без поддержки в данных."],"defense":"Признаю поражение по существу: три уязвимости независимы и каждая убивает идею. Уточнение к аргументу 1: таргет — будущий амплитудный режим, а не знак; MI по амплитуде проходит (p=0.005, 2.1). Это не спасает: связь с будущим движением ≈ 0, а режим уже объясняется текущими временем+ATR — предвестниковой премии над базлайном не видно, и предложенный тест ΔAUC именно это и проверил бы. Остаток ценности — не торговый, а диагностический: ретроспектив сам ставит задачу 'причина сдвига 2023, режимная диагностика на OHLC и телеметрии спреда'. Протокол falsification корректен и дёшев (1–3 дня) — его стоит сохранить как диагностический тест причин перелома, но не как кандидата с early warning и защитным позиционированием."}
```

### Раунд 3 — итог критика
```json
{"name": "Regime transition early warning system", "pros": ["Автор признал поражение по существу и уточнил таргет: будущий амплитудный режим, а не знак движения; единственный измеренный положительный сигнал — MI по амплитуде (p=0.005, отчёт 2.1).", "Уточнение корректно ограничивает претензию: сам автор показал, что амплитудный сигнал не конвертируется в предсказание будущего движения (≈0).", "Остаточная ценность — диагностическая: ретроспектива корректно ставит вопрос о причине сдвига 2023; протокол фальсификации дёшев (1–3 дня) и сохраняется как диагностический тест, а не как торговый кандидат."], "cons": ["Размер выборки неустраним: в ряде 2004–2026 один значимый перелом (граница 2023); классификатор с 1–3 позитивными примерами необучаем и невалидируем.", "CUSUM/Chow/BOCPD ретроспективны: лаг ≥1 бар — свойство класса методов, kill-критерий «лаг < 1 бар» априори недостижим; «early warning» как прогноз ни разу не подтвердился.", "Происхождение edge противоречит фактам: расширение обучения не помогает (2.8), режим объясняется временем+ATR (2.11), корреляция с будущим движением ≈0 (2.1)."], "novelty": {"duplicate_of": null, "retrospective_ref": null}, "vulnerabilities": ["Все три уязвимости раунда 1 признаны автором фатальными и неустранимыми; каждая независимо убивает идею: выборка из одного перелома, ретроспективность класса методов, отсутствие связи с будущим движением.", "Уточнение таргета на амплитуду не спасает: даже проходящий MI (p=0.005) не даёт edge на будущем движении, режим уже объясняется временем+ATR."], "falsification": {"protocol": "Сохранить как диагностический тест причин перелома 2023 (1–3 дня): проверка MI по амплитуде (p=0.005, отчёт 2.1) и подтверждение объяснения режима временем+ATR (2.11).", "result": "По существу пройдена: автор признал поражение; тест остаётся в диагностической роли и на торговый вердикт не влияет.", "cost": "1–3 дня"}, "potential": 1, "potential_why": "Механизма «кто платит» нет: режим объясняется временем+ATR, связь с будущим движением ≈0, а проходящий амплитудный MI (p=0.005) не конвертируется в устойчивый edge.", "verdict": "убита", "rationale": "Автор признал все три уязвимости фатальными и неустранимыми, и по существу это подтверждается: выборка из одного перелома, ретроспективность класса методов и отсутствие связи с будущим движением независимо убивают идею, а уточнение таргета на амплитуду не создаёт торгового edge."}
```

## Cross-asset relative strength signals [кластер 6]

- Статус: полный спор (3 раунда)
- Идей в кластере: Cross-asset relative strength signals; Synthetic Control Anomaly; Cross-sectional FX rank selection
- Блок идеи:
```
Название: Cross-asset relative strength signals
Суть: Use correlated currency pairs (EUR/USD vs USD/CHF, AUD/USD vs NZD/USD) to predict relative movement. Target is spread between correlated assets, not absolute direction. Pairs trading logic applied to FX correlations.
Обходимый тупик: regime drift
Откуда edge: [гипотеза] Correlated assets have temporary divergences that revert; relative strength is more stable than absolute direction because it cancels common factors (USD strength, risk sentiment).
Теги: 
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":null,"retrospective_ref":null},"vulnerabilities":["Происхождение edge против информационной границы проекта: спред — производный ряд, и предсказание его знака остаётся предсказанием направления. Ретроспектива установила отсутствие знаковой информации в данных (2.1: MI direction FAIL, p=0.229; 2.11: направление на next-open ≈0, гейт 0.10 не пройден; 2.11 'Already Moved': на H3 у 57% строк движение уже состоялось; раздел 4: 'Прямое предсказание направления — провалено во всех постановках'). Утверждение 'relative strength стабильнее абсолютного направления' — постулат, а не факт: edge возможен только через стационарность спреда (стат-арб), но тогда вход происходит ПОСЛЕ уже состоявшегося движения (z-score смещения — это прошлое движение, ловушка 2.11), и исход целиком зависит от реверсии vs продолжения — реверсия постулируется без доказательств.","Исполнение и стоимость: двухногая сделка удваивает спред, проскальзывание и requote-поверхность. Известные факты проекта: spread 0.8 уничтожает большинство кандидатов (2.12), requote ERROR-138 блокирует даже одиночные входы (2.5), MT4-результаты верифицируются вручную и расходятся с Python-бенчмарком (2.6), MT4 и MT5 дают разные события (2.13), одноисточниковый OHLC меняет итоги (2.12, 2.13, раздел 7). Требование PF≥1.3 с bootstrap CI lower>1.0 при удвоенных издержках и синхронизации котировок двух инструментов — принципиально более высокая планка, а кросс-инструментальная синхронизация баров/тиков в проекте не решена.","Стационарность и заявленный 'обходимый тупик': вычитание общего фактора не обходит regime drift, а само от него зависит — корреляция/коинтеграция пар режимно-нестабильна (перелом 2023, 2.8; risk-off/on; для USD/CHF — SNB 2015). Спред некоинтегрированных рядов — артефакт ложной регрессии. Выбор пары, окно оценки β и z-score — это снова автовыбор winner-конфигурации, который проект систематически переобучался (2.4, 2.12); при N сделок в десятки bootstrap CI широкие (2.2), а кросс-секционный вариант кластера (rank selection) добавляет multiple testing по 20–30 парам."],"falsification":{"test":"Двухэтапный убивающий тест. Этап A (дёшево): коинтеграция/стационарность спредов EUR/USD−USD/CHF и AUD/USD−NZD/USD: ADF на спреде, коэффициент β зафиксирован до 2022, проверка только на OOS 2023–2026. Этап B: честный бэктест относительной силы: сигнал — z-score спреда, вход next-open (live-safe постановка 2.14), двухногая сделка с заполнением обеих ног, спред 0.4 на каждую ногу, bootstrap CI, OOS 2023–2026, контроль календарного hour-baseline (2.12). Для соседей кластера тот же тест: Spearman относительной силы, применённый кросс-секционно к рангам пар вместо спреда двух пар.","metric":"PF на OOS + bootstrap CI lower bound; Spearman(предсказанная относительная сила, реализованное изменение спреда); ADF p-value","kill_threshold":"ADF p > 0.05 на OOS (механизма возврата к среднему нет) ИЛИ PF OOS < 1.3 с CI lower ≤ 1.0 ИЛИ Spearman ≤ 0.10 — порог направления проекта из 2.11 (гейт 0.10), с учётом удвоенного спреда на обе ноги","cost":"2–4 дня: данные второй пары частично уже есть (Nero/USDCHF и H1-файлы EURUSD в репозитории по cross-instrument-планам) — часы; реализация двухногого бэктеста с bootstrap CI — 2–3 дня","falsifiable":true},"preliminary_verdict":"условно"}
```

### Раунд 2 — ответ автора
```json
{"concession":false,"accepted_arguments":["«Relative strength стабильнее абсолютного направления» — постулат, а не факт; реверсия не доказана до ADF","Прогноз знака спреда — форма предсказания направления, подпадает под информационную границу (2.1, 2.11, разд. 4)","Исполнение: удвоенный спред, requote (2.5), нерешённая кросс-инструментальная синхронизация котировок","Коинтеграция режимно-нестабильна (2023, SNB-2015); автовыбор пары/β/z-score — риск multiple testing"],"defense":"Принимаю пункты выше по существу: идея действительно слабее, чем заявлено. Но ретроспектива закрывала направление одиночного инструмента по его собственной истории (фрактальные/ML-признаки); коинтеграционная реверсия кросс-спреда — иной, не тестированный механизм. Stage A (ADF на OOS 2023–2026, β и пары зафиксированы до 2022) дёшево и однозначно отвечает, есть ли реверсия. Ловушка 2.11 «Already Moved» не эквивалентна: сделка сознательно входит после отклонения, ставка — «реверсия vs продолжение», и именно это измеряет тест; корреляция с будущим движением ≈0 здесь не противоречит механизму. Исполнение — согласен, критично: даже при edge удвоенные издержки и отсутствие синхронизации баров двух инструментов могут сделать идею непрактичной в MT4. Но Stage B сперва отвечает на «есть ли edge в принципе» честной постановкой (next-open, 0.4/нога, bootstrap CI, hour-baseline). Кросс-секционный вариант (20–30 пар) отбрасываю как multiple testing; остаётся фиксированная пара с OOS-гейтом. Вердикт: условный, дешёвый убивающий тест оправдан."}
```

### Раунд 3 — итог критика
```json
{"name": "Cross-asset relative strength signals", "pros": ["Защита содержательная: приняты по существу все пункты атаки — нет оверклейма, дисциплина эксперимента честная", "Stage A (ADF на OOS 2023–2026, β и пара зафиксированы до 2022) — дешёвый и однозначно фальсифицируемый тест: он действительно изолированно измеряет наличие реверсии спреда", "Отказ от кросс-секции 20–30 пар как multiple testing — методологически верно; фиксированная пара с OOS-гейтом корректнее", "При прохождении теста есть ясная логика входа: сознательный вход после отклонения, ставка «реверсия vs продолжение» — то, что измеряет тест"], "cons": ["Механизм «кто платит» (edge_source) не назван и не проверен: если реверсия реальна, её уже арбитражат институциональные relative-value дески; после вычета розничных издержек остатка может не быть", "Признано, что «relative strength стабильнее» — постулат; стабильность спреда ≠ устойчиво торгуемая реверсия", "Исполнение признано критичным: удвоенный спред, проскальзывание, requote и кросс-инструментальная синхронизация могут сделать идею непрактичной в MT4 даже при доказанной реверсии", "Коинтеграция режимно-нестабильна (SNB-2015, 2023); одно OOS-окно на одной паре — малый N (десятки реверсий), низкая статистическая мощность", "Информационная граница не обойдена: знак спреда — форма предсказания направления; переформулировка «сознательный вход после движения» легитимна, но не отвечает, что реверсия уже учтена рынком", "Механизм — классический pairs trading / статистический арбитраж, новизны в механизме нет"], "novelty": {"duplicate_of": null, "retrospective_ref": null}, "vulnerabilities": ["«Кто платит» не установлен: неясно, чьи потери формируют устойчивый доход", "2.11: вход после состоявшегося отклонения остаётся входом после движения; тест должен показать доходность ПОСЛЕ издержек, а не только наличие реверсии", "Один OOS-период на фиксированной паре: возможны ложный kill или ложное выживание из-за режимности и малого N", "Издержки-барьер: даже стационарный спред неторгуем при удвоенном спреде в MT4 — автор сам это признал"], "falsification": {"null_hypothesis": "Спред фиксированной пары (EUR/USD vs USD/CHF или AUD/USD vs NZD/USD) не реверсирует на OOS 2023–2026: ADF не отвергает единичный корень при β и паре, зафиксированных до 2022", "decisive_test": "Stage A: ADF на OOS с предзафиксированными парой и β; при отвержении единичного корня — бэктест входа после отклонения z-score с вычетом удвоенного спреда, проскальзывания и requote; контроль устойчивости по подпериодам 2023/2024/2025", "kill_conditions": "Единичный корень не отвергнут; или реверсия есть, но PnL после издержек ≤ 0; или β/пара требуют переподгонки на OOS (режимный дрейф); или реверсия исчезает в отдельном подпериоде", "confidence": "Высокая по вопросу существования реверсии; тест не отвечает на «кто платит» и на устойчивость edge после издержек и конкуренции — для вердикта об edge обязателен бэктест с издержками"}, "potential": 2, "potential_why": "Механизм «кто платит» не назван; при реальной реверсии её уже собирают институциональные арбитражеры, а при признанном удвоении издержек в MT4 устойчивого остатка не видно — спор новых данных об edge не дал", "verdict": "условно", "rationale": "Защита содержательная: слабости приняты по существу, предложен дешёвый однозначный убивающий тест с OOS-гейтом и отказом от кросс-секции — но новых фактов об edge нет и «кто платит» не назван, поэтому потенциал низкий, а вердикт остаётся условным до результата ADF с учётом издержек"}
```

## Transfer Entropy Cross-Asset [кластер 7]

- Статус: полный спор (3 раунда)
- Идей в кластере: Transfer Entropy Cross-Asset; Cross-asset lead-lag signals
- Блок идеи:
```
Название: Transfer Entropy Cross-Asset
Суть: Вычислять transfer entropy (непараметрическая мера направленного информационного потока) между парами валют для обнаружения лидеров и запаздывающих. Использовать TE-профиль как признак: если одна пара «ведёт», другая следует с задержкой.
Обходимый тупик: already moved
Откуда edge: [гипотеза] transfer entropy улавливает нелинейные направленные зависимости, которые не видны в корреляции; информационное лидерство между активами — устойчивый микро-эффект.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{
  "novelty": {
    "duplicate_of": null,
    "retrospective_ref": null
  },
  "vulnerabilities": [
    "Уже-случившееся-движение (2.11, 2.14): механика «лидер двинулся → последователь повторит» по конструкции торгует ПОСЛЕ того, как движение стало видимым. Ретроспектива: на H3 у 57% строк ≥50% движения уже состоялось, корреляция с будущим движением при входе на next-open ≈ 0 (Spearman −0.0149/−0.0336; направление ≤ 0.0203 на val_stop), MI(direction) 0.003–0.004 bits FAIL (2.1). TE-детект лидерства запаздывает на бары — к моменту входа остаточное движение последователя уже учтено ценой; кластер воспроизводит ту же ловушку, что direction FAIL.",
    "Данные и стационарность: TE — непараметрическая мера, чувствительная к нестационарности и синхронному comovement. Информационное лидерство в FX документировано в литературе в основном на внутридневных (минуты/секунды) горизонтах; проект работает на H1/H3/H12/24H, где пары двигаются от одних и тех же новостей одновременно. Календарная доминантность (2.12: no-ML hour-baseline ≥85.9% PF лучшего ML) означает, что на этих масштабах TE измерит общую синхронную информацию (спурiозный перенос от общего драйвера), а не торгуемый направленный лаг. Данные — одноисточниковый OHLC с режимными переломами (2004–2026, перелом на границе 2023, секция 7): TE-оценки по неустойчивым окнам смещены, а смена источника OHLC уже меняла PnL (2.12, 2.13).",
    "Мультитестинг и размер выборки: сетка пары × лаги × масштаб бара × параметры TE-оценки (число лагов эмбеддинга, окно) — классический путь автовыбора winner, который в проекте систематически переобучался (2.4, 2.12); кандидатские N — десятки, bootstrap CI широкие (секция 7). Даже если TE-эффект реален как «микро-эффект», порог PF ≥ 1.3 с CI_lower > 1.0 при спреде 0.8 (убивает большинство кандидатов, 2.12) при малых N статистически недостижим. Плюс прецедент 2.4: cross-instrument (перенос правила между инструментами) дал результат только для XAGUSD/USDCHF."
  ],
  "falsification": {
    "test": "Двухступенчатый. Шаг 1 (дёшево): на train-окне (до 2022-12, чтобы не трогать eval с режимным переломом 2023) оценить TE(лидер→цель) для всех пар × лагов 1–24 бара × масштабов H1/H3/H12 на каноническом OHLC; нуль-распределение TE строить перестановкой/временным сдвигом ряда лидера; пару и лаг зафиксировать ДО просмотра eval. Шаг 2 (решающий): фиксированный TE-признак/правило использовать как направленческий сигнал для целевой пары на frozen eval 2023–2026 (вход next-open, спред 0.8, live-safe контракт хронологии fill из 2.12/2.5); метрика — PF с bootstrap CI, плюс проверка, что edge не объясняется календарём (сравнение с hour-baseline).",
    "metric": "Шаг 1: permutation p-value лучшего TE против суррогатов. Шаг 2: PF на frozen eval 2023–2026 со спредом 0.8 и bootstrap CI (нижняя граница).",
    "kill_threshold": "Шаг 1: лучший TE-эффект незначим (p ≥ 0.05) на всех масштабах и лагах → убита (не существует измеримого лидерства на горизонтах проекта). Шаг 2: PF < 1.3 или CI_lower ≤ 1.0 → убита (порог успеха проекта, секция 1; не спасает даже значимый TE, если он не конвертируется в прибыль после спреда и хронологии fill).",
    "cost": "3-5 дней",
    "falsifiable": true
  },
  "preliminary_verdict": "условно"
}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["already-moved (2.11, 2.14): механика «лидер двинулся → последователь повторит» торгует направление после видимого движения; TE-детект лидерства запаздывает на бары, и при входе next-open остаточное движение уже в цене — это та же ловушка, что direction FAIL, тупик гипотеза не обходит","синхронный comovement на H1–H24 (2.12): пары двигаются от общих новостей, TE измерит спуриозный перенос общего драйвера, а не торгуемый лаг"],"defense":"Цитаты из 2.1/2.11/2.14 точны: направленный сигнал отсутствует при входе на next-open, все постановки провалены. Признаю — моё допущение торгуемого лага между парами на часовых масштабах несостоятельно, и это неустранимо в рамках проекта (одноисточниковый OHLC, нет тиковых минутных горизонтов, где лидерство документировано). Частичная защита по остальным пунктам: (1) falsification-дизайн корректен — пара/лаг фиксируются до eval, permutation-нули дёшево отсекают шум, но common-driver-конфаунд потребовал бы conditional TE — уточнение, не спасение; (2) риск мультитестинга снижен пре-регистрацией, высокий kill-порог осознанно консервативен, но при малых N недостижим — согласен. Главное неустранимо: остаточного направленного сигнала нет ни внутри инструмента, ни между инструментами."}
```

### Раунд 3 — итог критика
```json
{"name": "Transfer Entropy Cross-Asset", "pros":["TE — легитимная непараметрическая мера направленного потока информации, обобщение корреляции; на внутридневных горизонтах лидерство между активами действительно документировано в литературе","Falsification-дизайн был корректен: пара и лаг фиксируются до eval, permutation-нули, пре-регистрация снижает риск мультитестинга","Защита автора содержательна и честна: признаны неудобные факты без попытки их обойти"], "cons":["Центральная предпосылка — торгуемый лаг между парами на H1–H24 — признана автором несостоятельной и неустранимой на данных проекта (одноисточниковый OHLC, нет минутных горизонтов)","Механика «лидер двинулся → последователь повторит» торгует после видимого движения; при входе на next-open остаточное движение уже в цене — та же ловушка, что direction FAIL, которую идея заявляла обойти","Синхронный comovement от общих новостей: TE измерит спуриозный перенос общего драйвера, а не торгуемый лаг; для разделения нужен conditional TE — уточнение, не спасение","Лидерство документировано на внутридневных горизонтах, проект работает на H1/H3/H12","Сетка пары×лаги×масштабы — риск мультитестинга, малые N, широкие CI, спред 0.8"], "novelty": {"duplicate_of": null, "retrospective_ref": null}, "vulnerabilities": ["already-moved (фатально, признано): направленный сигнал отсутствует при входе next-open, тупик не обходится","синхронный comovement / common-driver конфаунд (фатально, признано): TE измеряет общий драйвер, а не лаг","данные: одноисточниковый OHLC без минутных горизонтов, где лидерство документировано — исправление вне возможностей проекта","мультитестинг по парам×лагам×масштабам при малом N"], "falsification": {"design": "пара и лаг фиксируются до eval; permutation-нули; для отделения common-driver конфаунда необходим conditional TE", "outcome": "гипотеза фальсифицирована самим автором: остаточного направленного сигнала нет ни внутри инструмента, ни между инструментами, и это неустранимо"}, "potential": 1, "potential_why": "edge_source — чистая гипотеза без механизма «кто платит», а спор показал, что измеряемый эффект спуриозен на горизонтах проекта", "verdict": "убита", "rationale": "Признание автора содержательно и фатально: идея не обходит свой же тупик already-moved, торгуемого лага между парами на доступных данных не существует, а неустранимость делает гипотезу нереализуемой в рамках проекта."}
```

## Macro regime portfolio allocation [кластер 8]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Идей в кластере: Macro regime portfolio allocation; Ultra-low frequency regime bets
- Блок идеи:
```
Название: Macro regime portfolio allocation
Суть: Вместо торговли на каждом баре — классифицировать макро-режим (risk-on/risk-off/кризис) по spreads, rates, VIX и аллоцировать капитал между заранее подготовленными sub-strategies. Частота решений — раз в неделю/месяц.
Обходимый тупик: Режимный перелом 2023 (2.8, 2.9) и календарная доминантность (2.12)
Откуда edge: [гипотеза] макро-переключения управляются центральными банками и структурными потоками; платят те, кто остаётся в неправильном режиме при смене цикла
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":null,"retrospective_ref":null},"vulnerabilities":["Происхождение edge — непроверяемая гипотеза, прямо противоречащая ключевым выводам ретроспективы. Механизм требует предсказать смену макро-режима заранее, но единственный наблюдаемый перелом не предсказан ничем: 2.8 'val_select 2019–2022 PF 2.041 (532, 0 убыточных лет) → val_eval 2023–2026 PF 0.897 (357, 3 убыточных года), permutation p=1.000', 'расширение обучения не помогает, провал 2023–2026 устойчив'. Направленческой информации в данных нет (2.1: MI direction FAIL p=0.229, MI(direction) 0.003–0.004 bits, потолок R² 0.006–0.030), а риск-on/off-ставка — это по сути знак месячной/недельной доходности: тот же убитый класс, перенесённый на низкую частоту и на внешние переменные (VIX/rates/spreads), которых в проекте нет вообще (в DATA только OHLC-файлы, сбор макро-данных не начинался).", "Статистическая мощность несовместима с порогом проекта (PF ≥ 1.3 + bootstrap CI нижняя граница > 1.0). При решениях раз в неделю/месяц за 2004–2026 имеем ~270 недельных или ~63 месячных ставок, разбитых на 2–3 режима — это 20–40 независимых наблюдений на режимную ячейку; bootstrap CI будет шире искомого эффекта (секция 7: 'малые N дают широкие bootstrap CI и хрупкую годовую стабильность'). Малые N уже многократно убивали кандидатов (2.2: N=36; 2.4: N=44–48). Тот же представитель уже убит арбитражем проекта: docs/archive/brainstorm-filtered_deep.md, кластер 20 'Macro regime portfolio allocation' — вердикт 'убита', аргументы 'нет базового edge; частотный mismatch (H3 против weekly/monthly); недостаточная мощность; drift классификатора', автор признал аргумент в раунде 2.", "Нет sub-strategies для аллокации, и смена режима в выборке — N=1 событие. 'Заранее подготовленные sub-strategies' в проекте не существуют: за 6 месяцев ни одна система не прошла порог успеха, стабильные находки (движение-фильтр 2.11, baseline A 2.4) не режимные и research-only; аллокация между неработающими компонентами edge не создаёт, а удваивает неопределённость. Единственный перелом (2023) в данных один — режимный классификатор на одном событии не валидируется (та же причина убийства 'Regime transition early warning', архив, кластер 3: 'N=1 событие; фильтр реактивен, не даёт alpha'). Плюс режим в проекте уже объяснён простыми переменными: 2.11 'Amplitude Regime — режим объясняется временем+ATR'."],"falsification":{"test":"Собрать внешние макро-данные (VIX, ставки ЦБ, кредитные спреды) с корректным лагом публикации; построить режимный классификатор (risk-on/risk-off/кризис) строго по данным, доступным на момент t (без lookahead); ежемесячная аллокация между long-risk-on и защитной позицией на EURUSD по цене закрытия месяца; строгий временной сплит: обучение до 2022, OOS 2023–2026 + walk-forward; bootstrap CI.","metric":"Hit-rate правильного знака месячной ставки против 0.5; Spearman IC(лаггированные макро-переменные → forward месячная доходность) с 95% bootstrap CI; PF с учётом спреда/проскальзывания и нижняя граница CI.","kill_threshold":"Идея убита, если на OOS: |IC| ≤ 0.03 с CI, включающим 0, ИЛИ hit-rate ≤ 0.52 (n≈36–48 месячных ставок на 2023–2026), ИЛИ PF < 1.3 при CI_lower ≤ 1.0.","cost":"3–5 дней","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["арбитраж архива (к20): идентичная идея уже убита с теми же аргументами, признанными автором в раунде 2 — это неустранимо","2.1/2.8: edge требует предсказания знака режима, но направленческой информации в данных нет (MI direction FAIL, perm p=1.000 на единственном переломе 2023)","статистическая мощность: ~63 месячных ставки на 2–3 режима — десятки наблюдений на ячейку, недостижим порог PF≥1.3 с CI>1.0 (малые N уже убивали 2.2, 2.4)","нет sub-strategies для аллокации; перелом в выборке — N=1 событие, классификатор не валидируется"],"defense":"Признаю поражение по всем пунктам. Аргумент про архив фатален и самодостаточен: та же идея уже прошла арбитраж проекта с идентичной мотивировкой, и я её принял в раунде 2 — повторная подача без нового факта некорректна. По существу: механизм edge требовал предсказать знак будущей доходности на месячном горизонте, а ретроспектива это закрывает — направленческой информации нет (2.1), единственный перелом не предсказан ничем (2.8). Перенос убитого класса на низкую частоту и на внешние переменные (отсутствующие в проекте) — не новый канал, а переупаковка. Мощность при 63 ставках принципиально несовместима с порогом проекта. Механизм «кто платит» тоже признаю слабым: он не фальсифицируем на имеющихся данных и не подкреплён. Идея не проходит — отзываю."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Path morphology classification [кластер 9]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Идей в кластере: Path morphology classification; Topological Persistence Features; Fractal structural similarity
- Блок идеи:
```
Название: Path morphology classification
Суть: Classify price path shapes (trending, mean-reverting, spiking, consolidating) using shape descriptors (fractal dimension, Hurst exponent, path efficiency). Different exit strategies for each morphology. Predict morphology, not outcome.
Обходимый тупик: already-moved
Откуда edge: [гипотеза] Different path morphologies have different statistical properties; trending paths favor momentum exits, mean-reverting paths favor contrarian exits. Morphology is more predictable than direction.
Теги: 
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Кластер 9 повторяет механизм секций 2.9–2.11: геометрическо-фрактальные дескрипторы пути как предиктор режима/морфологии вместо направления. Прямое подтверждение в 2.10 и 2.11; амплитудный режим (=грубая морфология: движение/консолидация) уже классифицировался и предсказывался — результат заморожен как research-only, потому что не даёт торгового edge.","retrospective_ref":"Секция 2.10: «6.1 H12 геометрия MODEL_GATE_FAILED (AUC 0.53, delta +0.0026..+0.0048 при пороге ≥+0.02); 6.2 price action TRADING_GATE_FAILED (standalone PF 1.307, perm p 0.160 при гейте 0.10)»; Итог: «фрактальная геометрия, price action и outcome-TB не дают торгового сигнала». Секция 2.11: «07-07 Fractal Seq Transformer — амплитуда проходит pivot (val_select 0.31–0.34/val_eval 0.25–0.30), направление нет»; «07-08 Direction Inside Regime FAIL — bal_acc 0.5792→0.5287». Секция 2.9: «структура — главный носитель (no_structure рушит AUC до 0.534/0.500)». Механизм «предсказывать морфологию/режим, а не исход» уже реализован (амплитудный режим, fractal-признаки, movement filter — секция 3: «единственный стабильно воспроизводимый edge», заморожен) и на порог PF ≥ 1.3 не вышел."}, "vulnerabilities":["Already-moved/тавтология метки: морфология определяется по тому же окну цены, которое описывают fractal dimension/Hurst/path efficiency — классификация кодирует уже состоявшееся движение. Ретроспектива 2.11 (07-02): «Spearman из fractal0 0.8786, с будущим движением ≈0 (−0.0149/−0.0336)»; у 57% строк H3 ≥50% движения уже реализовано. Разные «морфологически-условные» выходы будут выглядеть как edge ровно потому, что условия кодируют прошлое движение; при переносе окна меток строго до входа сигнал схлопывается — та же судьба, что у already-moved и у направления.", "Режим предсказуем, но не торгуем — доказано в ретроспективе: амплитуда (ближайший аналог морфологии) проходит MI и pivot (2.1: MI amplitude PASS p=0.005; 2.11: амплитуда 0.3414→0.4449 выживает), направление внутри режима FAIL (2.11: bal_acc 0.5287, 2026 0.4747). Морфология — перекодировка амплитудного режима (в 2.11 «режим объясняется временем+ATR»): условные выходы могут лишь перераспределить PnL внутри режима, не добавляя информации; без направления любое перераспределение даёт ожидаемый PF ≈ 1.0 минус спред. Спред 0.8 уже уничтожает кандидатов (2.12).", "Дробление выборки и хрупкость слоя выхода: 4 класса морфологии → N на класс в десятки сделок, а ретроспектива прямо фиксирует «кандидатские N — десятки» как причину провала bootstrap CI и годовой стабильности (2.2, 2.4); автовыбор winner-правила нестабилен по сидам (2.4, 2.12: 2/5 сидов). Календарная доминантность (2.12: «no-ML hour-baseline ≥85.9% ML PF, все winner-правила time_only») — морфологический сигнал обязан сначала победить time-only базлайн. Наконец, выход — именно тот слой, где живёт артефакт хронологии fill (2.12: «после фикса хронологии fill PnL → −530.51, PF 2.82–3.42→0.82–0.94»)."], "falsification":{"test":"На locked-контракте (fixed11, 2022-12..2026-06, 9463 строки, спред 0.4, фиксированная хронология fill) определить 4 класса морфологии по окну СТРОГО до входа (бары ≤ entry−1, запрещено перекрытие с торговым баром). Обучить/настроить условные выходы на val_select, зафиксировать до оценки, прогнать на val_eval. Контроль: (1) лучший безусловный выход; (2) перестановка меток морфологии (permutation, ≥1000); (3) bootstrap CI на delta PF; (4) hour/calendar baseline для каждого класса.","metric":"delta PF = PF(условный выход) − PF(лучший безусловный выход) на val_eval со спредом 0.4; нижняя граница bootstrap CI дельты; permutation p; N сделок на класс; сравнение с hour-baseline.","kill_threshold":"Идея убита, если: нижняя граница bootstrap CI delta PF < +0.10 ИЛИ permutation p ≥ 0.05 ИЛИ любой класс имеет n < 100 сделок ИЛИ любой класс не превосходит hour-baseline PF. Автоматическая гибель при использовании в метках баров, перекрывающихся со входом (already-moved), или при любой зависимости edge от хронологии fill, не покрытой контрактом.","cost":"3–5 дней (гигиена меток и окон: 1 день; классификатор + permutation + bootstrap: 1–2 дня; сетка условных выходов с фиксацией до оценки: 1–2 дня; инфраструктура locked-контракта и fill-хронологии уже есть в проекте)","falsifiable":true}, "preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession": true, "accepted_arguments": ["Пункт 1 (дубликат): подтверждён цитатами 2.10 и 2.11 — фрактальные дескрипторы как предиктор режима уже реализованы; амплитудный режим (=грубая морфология) классифицировался и заморожен research-only, торгового edge не дал. Моя морфология — перекодировка того же режима, novelty нет.", "Пункт 2 (already-moved): фатален и неустраним. Цитата 07-02 из 2.11: Spearman fractal0 с прошлым движением 0.8786, с будущим ≈0 (−0.0149/−0.0336), 57% строк H3 с ≥50% реализованного движения. Моя идея заявляла обход already-moved, но её собственный механизм — дескрипторы пути из того же окна, что и метка, — и есть already-moved: морфологическое условие кодирует состоявшееся движение. Тавтология метки неустранима по построению.", "Пункт 3 (кто платит + малые N): без направления условные выходы лишь перераспределяют PnL внутри режима → ожидаемый PF≈1.0 минус спред 0.8 (2.12); плюс 4 класса при десятках сделок ломают bootstrap CI (2.4, 2.12)."], "defense": "Защита по существу невозможна: ретроспектива не оставляет ни одной опоры. Фатально не одно уязвимое место, а совпадение трёх фактов: (1) механизм уже был проверен и закрыт (2.10: фрактальная геометрия MODEL_GATE_FAILED; 2.11: амплитудный режим — research-only), (2) сам инструмент идеи — дескрипторы пути — эмпирически доказанно коррелирует с прошлым движением и ≈0 с будущим (07-02), то есть заявленный обход already-moved опровергнут той же цитатой, и (3) «кто платит» не прояснён: без направленческой информации перераспределение выходов внутри режима в пределе даёт PF спреда. Уточнение не спасает: любая формулировка метки по барам до входа уничтожает корреляцию дескрипторов с будущим (0.8786→≈0), то есть условный выход лишается источника информации. Дробление на 4 класса усугубляет нестабильность (2.4, 2.12). Признаю: идея убита фактами ретроспективы, falsification-контракт корректен, вердикт «убита» обоснован."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Conditional return distribution modeling [кластер 10]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Блок идеи:
```
Название: Conditional return distribution modeling
Суть: Model full conditional distribution of returns (not just mean) using mixture density networks or quantile regression. Trade when distribution shows asymmetry or fat tails. Target is distribution shape, not point forecast.
Обходимый тупик: low R², already-moved
Откуда edge: [гипотеза] Return distributions are time-varying and conditionally asymmetric; capturing higher moments (skewness, kurtosis) provides edge even when mean return is unpredictable.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Механизм edge идентичен Entry Path Quantile (2.4): моделирование условного распределения доходности (quantile-головы q10/q50/q90 для ret_24_dir_atr, LightGBM objective='quantile') вместо точечного прогноза среднего, с торговым правилом на основе формы распределения (границ квантилей). Трек доведён до production-ready, затем отозван: нестабилен по сидам (2/5), median 8 сделок, research-only. Более того, сам источник edge ('условная асимметрия даёт сигнал при непредсказуемом среднем') прямо опровергнут: асимметрия распределения доходности в торговом правиле эквивалентна условному предсказанию знака, а знак предсказан не был ни маргинально (MI direction FAIL, 2.1), ни условно (Direction Inside Regime FAIL, 2.11).","retrospective_ref":"Секция 2.4: '04-10 quantile PASS-technical — val PF 11.05 (N=25), test inf (N=24); 04-12 quantile-status-decision **PASS → production-ready**: test N=48, PF 8.18, win 81.3%, 0 убыточных лет, MT4 parity 20/20 (PF 11.91); 04-13 quantile-forward-validation WATCH (trades=0)... Пост-audit (05-07, см. 2.5): quantile-правило нестабильно по сидам (2/5), median 8 сделок → research-only'. Коррелирующие цитаты: секция 4 — 'Прямое предсказание направления — провалено во всех постановках (см. 2.7, 2.11)... нулевая корреляция при входе на next-open. Гипотеза: в признаках нет информации о знаке (см. 2.1, MI direction FAIL)'; секция 2.11 — '07-08 Direction Inside Regime **FAIL** — bal_acc 0.5792→0.5287 (mcc 0.058), 2026 n=59 → 0.4747'; секция 2.6 — 'quantile-вариант REJECT (PF 0.18/95)... абсолютный probability-threshold мёртв'; секция 2.1 — 'Conformal (α=0.10): покрытие 90%, при жёстком θ эффекта нет'."},"vulnerabilities":["Асимметрия — это направление в маскировке. Гипотеза 'высшие моменты дают edge при непредсказуемом среднем' внутренне противоречива: для распределения доходности торговое правило 'long при правом скосе / short при левом' эквивалентно условному предсказанию знака. Проект дважды фальсифицировал условное знаковое содержание: MI direction FAIL (val p=0.229, MI(direction) 0.003–0.004 bits, 2.1) и Direction Inside Regime FAIL (bal_acc 0.5792→0.5287, 2.11). Если бы условная асимметрия предсказывала знак, она противоречила бы этим результатам; если не предсказывает — edge нет.","Контаминация таргета 'already-moved'. Распределение доходности на горизонте входа вычисляется на барах, где ≥50% движения уже состоялось (2.11: на H3 у 57% строк ≥50% движения реализовано; Spearman fractal0 0.8786 против будущего движения ≈0, −0.0149/−0.0336). Любая 'тяжёлохвостость/асимметрия' на входе с высокой вероятностью кодирует уже случившееся движение, а не будущее — ровно тот артефакт, который убил 2.11 и 2.12 (фикс хронологии fill обнулил PF 2.82–3.42→0.82–0.94).","Выборка, стационарность и исполнение. Условные оценки хвостов (skewness/kurtosis) сходятся на порядок медленнее среднего; у проекта кандидатские N — десятки (2.4: median 8 сделок; 2.6: N=95; 2.2: N=36–101), bootstrap CI будут широчайшими. Режимный перелом 2023 (2.8: 'провал 2023–2026 устойчив к расширению обучения') и календарная доминантность (2.12: hour-baseline догоняет ML PF) сломают калибровку распределения. Исполнение: вероятность-пороги мертвы ('абсолютный probability-threshold мёртв', 2.6), спред 0.8 убивает кандидатов (2.12), requote ERROR-138 (2.5), MDN-обучение расходится на GPU (2.5) — детерминизм только CPU."],"falsification":{"test":"На live-safe frozen-признаках (без future-derived полей, вход next-open, единый контракт внутрибаровой хронологии fill) обучить quantile-головы q10/q50/q90 целевой доходности (инфраструктура уже существует в ML/entry_path_v1_quantile) и вычислить статистику асимметрии на точке входа S=(q90−q50)−(q50−q10). Шаг 1 (диагностика): Spearman(S, sign(future_ret_next_open)). Шаг 2 (торговля): правило 'long при S>θ / short при S<−θ' с θ из train, спредом 0.8 и bootstrap CI на frozen OOS 2023–2026. Контроль: повторить с исключением уже-реализованного движения (вычисление S только по барам строго до входа) и с двумя вариантами хронологии fill.","metric":"Spearman(S, знак будущего движения от next-open); точность направления при θ из train; торговый PF с bootstrap CI (нижняя граница) на frozen OOS 2023–2026.","kill_threshold":"|Spearman| ≤ 0.02 (гейт направления из 2.11: направление ≤0.0203 на val_stop) или точность направления ≤ 0.52 — идея мертва. Торговый PF < 1.3 или нижняя граница bootstrap CI ≤ 1.0 — мертва. Любой PF ≥ 1.3, исчезающий при исключении already-moved-окон или при смене хронологии fill — артефакт постановки, мертва.","cost":"диагностика 1–2 дня (quantile-инфраструктура и live-safe контур уже есть); полный торговый контур со спредом и bootstrap — ещё 2–3 дня","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession": true, "accepted_arguments": ["novelty: идея — дубль 2.4 (quantile-головы q10/q50/q90, LightGBM quantile, правило по форме распределения); цитата подтверждена, трек дошёл до production-ready и отозван post-audit: нестабилен по сидам 2/5, median 8 сделок → research-only", "асимметрия = знак в маскировке: правило «long при правом скосе» эквивалентно условному предсказанию знака; знак фальсифицирован дважды — MI direction FAIL (p=0.229, 2.1) и Direction Inside Regime FAIL (bal_acc 0.5792→0.5287, 2.11); секция 4: «в признаках нет информации о знаке». Третьего пути для торгового правила нет — аргумент неустраним", "already-moved контаминация: 57% строк H3 с ≥50% реализованного движения, Spearman fractal0 0.8786 против будущего ≈0 (2.11) — форма распределения на входе кодирует прошлое"], "defense": "Признаю все три аргумента по существу. Идея не нова (2.4 — тот же механизм quantile-голов и правила по форме распределения, отозван за нестабильность), а её заявленный источник edge — условная асимметрия — логически сводится к предсказанию знака, которое проект фальсифицировал в двух независимых постановках. Контаминация already-moved добивает даже диагностическую часть. Единственный уцелевший фрагмент — компонент «fat tails» как фильтр волатильности — вырождается в уже подтверждённый амплитудный режим (2.11, 2.12), то есть это не данная идея, а уже существующий research-only сигнал без торгового слоя. Гипотезу считаю убитой: механизм не имеет подтверждённого edge, а его проверка (falsification-тест критика) — по сути повторный прогон гейта направления 2.11, который уже провален."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Liquidity event prediction [кластер 11]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Блок идеи:
```
Название: Liquidity event prediction
Суть: Predict liquidity events (spread widening, volume spikes, gap formations) rather than price movement. Target is liquidity state change. Trade when liquidity event probability is high, using market orders to capture dislocations.
Обходимый тупик: already-moved, low R²
Откуда edge: [гипотеза] Liquidity provision is costly; market makers demand compensation for adverse selection. Predicting when liquidity will dry up allows capturing the liquidity premium.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Идея — буквальная пересдача уже убитой «Liquidity event prediction [к6]» в /home/hohla/git/SoSimple/docs/archive/brainstorm-filtered_deep.md (арбитраж от 2026-08-15, строка 32 и 59): «Таргет ненаблюдаем; маркет-ордер — худшее исполнение; edge перевёрнут — розница платит премию за ликвидность; дубль уже убитой (brainstorm-filtered_qwen.md, строки 59, 79)», автор признал аргумент (раунд 2). Тот же текст (суть/edge/теги) в /home/hohla/git/SoSimple/docs/audit/brainstorm-ideas.md, строки 194–199.","retrospective_ref":"Точного аналога с ТЕМ ЖЕ механизмом edge в секциях 2/4 ретроспективы нет: там спред и исполнение фигурируют как издержки и блокеры, а не как механизм сбора премии за ликвидность. Ближайшие опорные факты: 2.13 «гипотеза „плохой fill брокера" отвергнута», fill rate↔PF Spearman −0.46; 2.5 requote ERROR-138, 8/87 входов не открылись; 2.12 спред 0.8→PF 1.32 и фикс хронологии fill уничтожил edge (PF 2.82–3.42→0.82–0.94). Механизм-аналог установлен не по ретроспективе, а по арбитражу проекта (к6, тот же класс «исполнение/ликвидность», убит ранее вместе с к4 Microstructure imbalance и к5 VPIN)."}, "vulnerabilities":["ДАННЫЕ: таргет не наблюдаем исторически. Главный класс событий (расширение спреда, дислокация) невозможно ни отмаркировать, ни смоделировать: DATA — только Bid-OHLC (time;open;high;low;close;volume;atr14), спред-канал в симуляторе константный (параметр 0.2/0.4/0.8, см. 2.12), тиков нет, bid/ask/spread есть лишь в телеметрии недавнего MT5-цикла (docs/schemas/mt5_open_position_feature_contract.md), а volume — source/tick volume, не биржевой (docs/superpowers/plans/2026-06-30-stage6_2-h12-price-action-feature-family.md: «treated as broker/source volume, not real exchange volume»). Обучать и валидировать на целевом классе событий не на чем; это же аргумент убийства к4/к5/к6/к17 в арбитраже.","ПРОИСХОЖДЕНИЕ EDGE: механизм перевёрнут относительно исполнения. Гипотеза «market makers demand compensation for adverse selection» описывает премию ПРОВАЙДЕРА ликвидности (тот, кто котирует и забирает спред). Исполнение идеи — маркет-ордера во время дислокации, т.е. ТЕЙКЕР: платит максимальный спред в момент, когда adverse selection максимален (спред расширяется именно из-за роста токсичности потока). Розница платит премию за ликвидность, а не собирает её; плюс маркет-мейкер/брокер видит тот же OHLC и при реальной предсказуемости уже заложил её в спред — розничный сигнал из того же ценового потока информационно слабее. Смежная гипотеза класса исполнения уже отвергнута проектом (2.13).","ВЫБОРКА/СТАЦИОНАРНОСТЬ/ИСПОЛНЕНИЕ: редкие события + максимальные издержки в точке входа. События ликвидности редки (weekly gap XAUUSD ~52/год; значимые расширения спреда — единицы-десятки в год), после frozen-протокола, multi-seed и yearly-pass N сожмётся до десятков — рецидив убийц проекта (2.2, 2.4: «кандидатские N — десятки», широкие bootstrap CI). Издержки в точке входа максимальны: дислокация = спред в пике (2.12: спред 0.8 уничтожает кандидатов), маркет-ордер в дислокацию = requote ERROR-138 и проскальзывание (2.5: онлайн Net −503.9 хуже tester −980.82), а внутрибаровая хронология fill в таком режиме не верифицирована (2.12: фикс хронологии уничтожил edge)."],"falsification":{"test":"Убивающий тест на механизме НЕ формулируется: целевой класс событий (расширение спреда/дислокация) не наблюдаем в данных проекта (только Bid-OHLC, константный спред в симуляторе без спред-канала, нет тиков/bid-ask истории за 2004–2026), поэтому ни маркировка таргета, ни честный расчёт PF «со спредом и проскальзыванием» в дислокацию невозможны — идея проваливает сам критерий приёмки проекта (секция 1) по неосуществимости, а не по измерению. Единственный прокси на маркируемых событиях (source-volume spike > p95, weekly gap; frozen-правило, OOS 2023–2026, вход next-open, спред 0.8) не проверяет механизм «премия за ликвидность при дислокации»: source-volume — счётчик тиков брокера, а не ликвидность; gap — иное явление; прохождение прокси не подтверждает механизм, провал прокси его не убивает.","metric":"— (прокси: AUC предсказания события на OOS + PF с bootstrap CI — но результат не решающий, измеряет не тот конструкт)","kill_threshold":"— (прокси: AUC ≤ 0.55 или PF < 1.3 / CI_lower ≤ 1.0 — даже провал не убивает заявленный механизм, т.к. измеряется не он)","cost":"неприменимо — данных для теста механизма нет; прокси-вариант ≈ 2–4 дня, но к вердикту не приводит","falsifiable":false},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["novelty: идея — точный дубль «Liquidity event prediction [к6]» (docs/archive/brainstorm-filtered_deep.md, стр. 32 и 59): те же аргументы (таргет ненаблюдаем, маркет-ордер — худшее исполнение, edge перевёрнут) и отметка «автор признал аргумент (раунд 2)». Это тот самый случай «точная цитата о том же механизме» — фатально и неустранимо.","ДАННЫЕ: таргет ненаблюдаем исторически — только Bid-OHLC, константный спред в симуляторе (0.2/0.4/0.8), тиков и bid-ask истории нет; маркировать целевой класс событий не на чем — идея неосуществима, а не просто не подтверждена.","ПРОИСХОЖДЕНИЕ EDGE: механизм перевёрнут — маркет-ордер в дислокацию это позиция тейкера: платишь пиковый спред в момент максимального adverse selection (2.5: requote ERROR-138, 8/87; 2.12: спред 0.8→PF 1.32; 2.13: fill↔PF Spearman −0.46).","ВЫБОРКА: редкие события (52 gap/год) + сжатие N после frozen-протокола — рецидив убийц 2.2/2.4."],"defense":"Защищать нечего. Критик прав по всем четырём пунктам, а главный — novelty — проверен мной по первоисточнику: это дословная пересдача уже убитой [к6], чей автор уже признал аргументацию в раунде 2. Формулировка «маркет-мейкеры требуют компенсации за adverse selection» описывает премию провайдера, но исполнение идеи — маркет-ордера в момент дислокации — ставит нас в позицию тейкера, платящего эту премию, а не собирающего её. Данные проекта принципиально не содержат целевого класса событий. Признаю: идея убита по неосуществимости, и повторная подача была ошибкой. Отказываюсь от гипотезы."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## PCMCI Causal Drivers [кластер 12]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Блок идеи:
```
Название: PCMCI Causal Drivers
Суть: Применить PCMCI (алгоритм causal discovery для временных рядов) для выявления причинно-следственных связей между кросс-инструментальными переменными (нефть, бонды, индексы) и целевой парой. Использовать только причинные связи как признаки, отсекая ложные корреляции.
Обходимый тупик: low R²
Откуда edge: [гипотеза] причинные связи между активами устойчивее корреляционных; causal filter устраняет шумовые признаки, повышая signal-to-noise без leakage.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"docs/archive/brainstorm-filtered_qwen.md (арбитраж 2026-08-15): та же идея PCMCI Causal Drivers [кластер 12] уже рассмотрена и убита — 'Информационный потолок direction (2.1: MI 0.003-0.004 bits); нестационарность causal graph (2.8: перелом 2023); данные и выравнивание (2.12: хронология уничтожила edge)'. Повторная подача без новых аргументов — не новизна, а рецидив.","retrospective_ref":"Прямого аналога с тем же механизмом (causal discovery между кросс-инструментами) в секциях 2/4 нет: 'causal' в 2.4 и 4 — это offline-гейт по строкам без future (анти-leakage), а не PCMCI; cross-instrument в 2.4 ('cross-instrument — только XAGUSD/USDCHF') — перенос правила, не причинный фильтр признаков. НО ретроспектива убивает премису идеи априорно: секция 7 'Информационная граница: маргинальный MI не даёт направления — сторона знака движения закрыта априорно'; 2.1 'MI Upper Bound: direction FAIL (val p=0.229)', 'MI(direction) 0.003–0.004 bits' против амплитуды 0.010–0.022; 2.4 'offline-гейт (all-rows/causal/direct-bar) PF≈1.0–1.16'."},"vulnerabilities":["Происхождение edge: идея не создаёт информацию, а фильтрует её. PCMCI-фильтр подчиняется Data Processing Inequality: причинные признаки несут не больше информации о знаке, чем исходные кросс-инструментальные ряды. Ретроспектива формально закрыла сторону знака (7: 'сторона знака движения закрыта априорно'; 2.1: direction MI 0.003–0.004 bits, MI-потолок R² 0.006–0.030 ниже legacy R² 0.084–0.18, который был leakage). Заявленный edge ('причинные связи устойчивее корреляционных') не адресует корень: low R² — это информационный потолок, а не дефект набора признаков, который можно 'отцедить' причинностью.","Стационарность и данные: PCMCI оценивает граф на полном окне в предположении стационарности, а ретроспектива фиксирует режимный перелом — 2.8: 'val_select 2019–2022 PF 2.041 → val_eval 2023–2026 PF 0.897, permutation p=1.000', 'провал 2023–2026 устойчив к расширению обучения'. Причинная структура нефть/бонды/индексы ↔ FX режимно-зависима (risk-on/off): граф, обученный на 2004–2022, мёртв в 2023–2026. Данные: 2.12 'одноисточниковый OHLC меняет результаты', 2.13 'MT4 и MT5 дают разные события'; бонды/индексы не имеют проверенного источника в MT4, а выравнивание сессий/праздников/выходных по инструментам создаст ложные причинные связи прямо на входе.","Исполнение и автовыбор: даже при реальных причинных связях цепочка discovery→признак→сигнал→сделка погибает на исполнении — 2.12: спред 0.8 (PF 3.27→1.32), фикс хронологии fill уничтожил edge (PF 2.82–3.42 → 0.82–0.94, 'весь edge зависел от некорректной хронологии'); 2.5: requote ERROR-138, 8/87 входов не открылись. PCMCI выполняет тысячи тестов условных независимостей → ложные связи; выбор 'причинных' признаков из множества кандидатов = автовыбор winner, систематически переобучающийся в этом проекте (2.4, 2.12); N живых кандидатов — десятки, bootstrap CI широк."],"falsification":{"test":"Кросс-инструментальный MI-потолок ДО построения пайплайна: собрать ряды нефть/бонды/индексы (частично есть: XAGUSD/XAUUSD из cross-instrument robustness), выровнять по времени с целевой парой, вычислить MI(direction | лаговые кросс-инструментальные доходности) на строгом out-of-sample через существующую MI-инфраструктуру (2.1) с permutation-тестом и bootstrap CI. Если MI не превышает потолок — идея убита без затрат на PCMCI, т.к. никакой причинный фильтр не добавит информацию о знаке, которой нет в исходных рядах.","metric":"MI(direction | cross-instrument) в битах + permutation p-value + верхняя граница bootstrap CI","kill_threshold":"Верхняя граница CI MI ≤ 0.005 bits (потолок direction из 2.1 — 0.003–0.004 bits) ИЛИ permutation p ≥ 0.05 — идея убита","cost":"1–2 дня","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["Информационный потолок (2.1): MI(direction) 0.003–0.004 bits, permutation p=0.229; секция 7 — 'сторона знака движения закрыта априорно'. DPI-аргумент верен: PCMCI-фильтр не создаёт информацию, а кросс-инструментальные ряды — те же рыночные данные, потолок применим и к ним.","Тот же механизм уже протестирован: 2.4 offline-гейт (causal) PF≈1.0–1.16 — причинный фильтр признаков edge не дал. Это точная ссылка ретроспективы на тот же механизм edge.","Рецидив: идея убита в кластере 12 (brainstorm-filtered_qwen), автор уже признал аргумент в раунде 2; новых аргументов не предъявлено."],"defense":"Идея убита по существу: edge был помечен как гипотеза и опровергнут информационным потолком, а не дефектом исполнения. Аргументы 2.8/2.12/2.5 (нестационарность, хронология, requote) вторичны — они бьют по любой кандидатуре и лишь добивают уже убитую. Единственное, что защищаю: falsification-тест остаётся дешёвым (1–2 дня, без затрат на PCMCI), использует существующую MI-инфраструктуру с permutation и bootstrap CI и закрывает более широкий вопрос — есть ли информация о знаке в экзогенных рядах вообще; критик сам признал тест в раунде 2. Но теперь тест нужен не для воскрешения идеи, а для диагностического закрытия направления кросс-инструментальных признаков."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Fluctuation Dissipation Entry [кластер 13]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Блок идеи:
```
Название: Fluctuation Dissipation Entry
Суть: Применить флуктуационно-диссипативную теорему из статистической физики: в равновесии отклик системы пропорционален флуктуациям. Отклонение от FDT-соотношения указывает на неравновесный режим, где цена более предсказуема.
Обходимый тупик: не указан
Откуда edge: [гипотеза] валютный рынок вблизи равновесия — efficient; отклонение от FDT — индикатор temporary disequilibrium, когда price impact предсказуем.
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Direction Inside Regime (2.11, 07-08) — тот же механизм edge: состояние/режим, определяемый по флуктуациям (амплитуде), используется как фильтр для предсказания знака движения; в 07-08 направление внутри амплитудного режима провалилось (bal_acc 0.5792→0.5287, mcc 0.058, 2026 n=59→0.4747). Механизм FDT-идеи идентичен: мера отклонения от равновесия (флуктуационно-диссипативное соотношение — производная от волатильности/флуктуаций) → режим → предсказуемость price impact (знака). Дополнительно перекрыт секцией 4: 'Прямое предсказание направления — провалено во всех постановках (см. 2.7, 2.11)... Гипотеза: в признаках нет информации о знаке (см. 2.1, MI direction FAIL)'.","retrospective_ref":"Секция 2.11: '07-07 Amplitude Regime — режим объясняется временем+ATR.' и '07-08 Direction Inside Regime FAIL — bal_acc 0.5792→0.5287 (mcc 0.058), 2026 n=59 → 0.4747.' Секция 4: 'Прямое предсказание направления — провалено во всех постановках (см. 2.7, 2.11)... Гипотеза: в признаках нет информации о знаке (см. 2.1, MI direction FAIL).' Секция 2.1: 'MI Upper Bound (08-11): amplitude PASS (p=0.005), direction FAIL (val p=0.229); MI(direction) 0.003–0.004 bits.'"}, "vulnerabilities":["1) Информационная граница: edge построен на предсказуемости ЗНАКА (price impact) в неравновесном состоянии, но направленческая информация в данных закрыта априорно — MI-аудит (2.1): MI(direction) 0.003–0.004 bits, MI-потолок R² 0.006–0.030, direction FAIL (p=0.229). Любой FDT-фильтр выборки не добавляет информации, которой нет в данных: он лишь сужает выборку до меньшего N, а не создаёт знак.","2) Механизм сводится к уже проваленному и уже найденному: мера флуктуаций в FDT — это реализованная волатильность/ATR; 'отклонение от FDT' — производный волатильно-автокорреляционный режим, коллинеарный амплитудному режиму, который 'объясняется временем+ATR' (2.11). Направление ВНУТРИ такого режима уже протестировано и FAIL (07-08). Вероятный исход: FDT-индикатор повторит календарную доминантность (no-ML hour-baseline ≥85.9% ML PF, 2.12), т.к. неравновесность в FX почти наверняка проксируется временем суток/волатильностью.","3) Нестационарность и редкие события: FDT требует стационарной оценки 'равновесных' флуктуаций на длинном окне, но проект зафиксировал устойчивый режимный перелом на границе 2023 (2.8: расширение обучения не спасает), а смешение режимов в одном 'равновесном' базисе испортит оценку FDT. События 'отклонения от равновесия' будут редкими → N десятки сделок → широкие bootstrap CI (2.2, 2.4); спред 0.8 уничтожает большинство кандидатов (2.12). Оценка FDT-параметров на нестационарных данных — ещё один источник призрачного edge."], "falsification":{"test":"Повторить протокол Direction Inside Regime (2.11, 07-08) с заменой режима на FDT-состояние: рассчитать FDT-индикатор отклонения (например, отношение автокорреляционного отклика к равновесным флуктуациям на скользящем окне), бинаризовать в 'неравновесие'/'равновесие', обучить классификатор знака ТОЛЬКО на строках FDT-неравновесия, проверить на frozen OOS со спредом. Параллельно — MI-аудит: MI(direction | FDT-состояние) против маргинального MI(direction).","metric":"MI(direction | FDT-состояние) в битах; торговый PF на строгом out-of-sample со спредом и bootstrap CI (нижняя граница); доля объяснённой дисперсии направления R² внутри FDT-состояния.","kill_threshold":"Идея убита, если MI(direction | FDT-состояние) ≤ 0.004 bits (не превышает маргинальный потолок 2.1) ИЛИ торговый PF < 1.3 при bootstrap-нижней границе ≤ 1.0 ИЛИ FDT-режим коллинеарен календарю (no-ML hour-baseline ≥ 85.9% от PF модели, как в 2.12).","cost":"3–5 дней (инфраструктура готова: frozen-контур 2.12, MI-методика 2.1, протокол Direction Inside Regime 2.11; нужно: расчёт FDT-индикатора ~0.5 дня, MI-аудит ~1 день, торговый тест на frozen OOS ~1–2 дня, multi-seed проверка ~1 день)","falsifiable":true}, "preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{
  "concession": true,
  "accepted_arguments": [
    "novelty.duplicate_of — механизм идентичен Direction Inside Regime (2.11): режим, построенный на флуктуациях, используется как фильтр знака; 07-08 FAIL. Цитаты проверены в retrospective.md и точны.",
    "vulnerabilities.1 — информационная граница: MI(direction) 0.003–0.004 bits (2.1) закрывает знак априорно; FDT-состояние — функция уже имеющихся признаков (волатильность/ATR), и фильтр выборки не создаёт информации о знаке, которой в данных нет."
  ],
  "defense": "Спасти идею может только один теоретический нюанс: FDT-отклонение — не ATR, а отношение отклика к флуктуациям (второй порядок: mean-reversion/волатильная автокорреляция), чего 07-08 не тестировал [гипотеза]. Но это не преодолевает потолок: даже новый режим остаётся фильтром знака, а условный MI(direction|FDT) ограничен маргинальным MI тех же признаков. «Кто платит» в идее не указан — слабость признаю, но она не фатальна. Фальсификационный тест критика корректен и дёшев (MI-аудит + frozen OOS), однако ожидаемый исход — калибровочный отказ; приоритет идеи низкий."
}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Амплитудно-календарный режимный отпечаток [кластер 14]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Блок идеи:
```
Название: Амплитудно-календарный режимный отпечаток
Суть: Амплитудный фильтр стабилен, календарная доминантность — факт. Вместо использования календаря как сигнала, календарно-амплитудное взаимодействие используется как идентификатор режима. Кластеризация по профилю (час×ATR-квантиль), торговля только в режимах, исторически соответствующих profitable-кластерам.
Обходимый тупик: regime drift: режимный перелом 2023 обнаруживается как смена кластерной структуры, а не как падение PF модели.
Откуда edge: [гипотеза] совместное распределение внутидневной волатильности и календарного времени формирует квази-стационарные режимы, переходы между которыми реже, чем переходы внутри режима.
Теги: 
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Механизм edge заявлен как «совместное распределение внутридневной волатильности и календарного времени формирует режимы» — это уже установленный факт проекта, и его торговая эксплуатация уже документирована как неработающая.","retrospective_ref":"секция 2.11: «07-07 Amplitude Regime — режим объясняется временем+ATR», «07-08 Direction Inside Regime FAIL — bal_acc 0.5792→0.5287 (mcc 0.058), 2026 n=59 → 0.4747»; секция 2.12: «no-ML hour-baseline ≥85.9% ML PF (медиана 0.98) — календарная доминантность; leaderboard 0/11 multi-seed, все winner-правила time_only/movement_plus_time»; секция 4: «Календарная доминантность (см. 2.12) — no-ML hour-baseline догоняет ML-PF, формальные winner-правила оказываются time_only; некалендарный сигнал не выделен»."},"vulnerabilities":["1) Происхождение edge: источник прибыли идеи (режим как функция времени и ATR) — это дословно подтверждённый факт проекта (2.11 «режим объясняется временем+ATR»), а не новый edge. Все известные проекту реализации этого механизма прибыли не дали: торговля внутри режима FAIL (2.11), движение-фильтр заморожен как research-only (2.11), hour-baseline догоняет ML-PF, winner-правила time_only и 0/11 выживают multi-seed (2.12), а единственный кандидат, формально прошедший locked test, умер после фикса хронологии fill (2.12). Идея переупаковывает известный механизм в новую обёртку (кластеризация), не добавляя источника прибыли.","2) Стационарность: тезис «квази-стационарные режимы, переходы реже, чем переходы внутри режима» — не проверенная гипотеза, и прямые данные ретроспективы ей противоречат: на границе 2023 структура прибыльности сменилась устойчиво и не лечится расширением обучения (2.8: val_select 2019–2022 PF 2.041 → val_eval 2023–2026 PF 0.897, permutation p=1.000; walk-forward не помогает; 2.9: провал 2023–2026 устойчив). Значит «исторически profitable-кластеры» — это ровно те кластеры, которые умерли в 2023. Замена детекции дрейфа с падения PF на смену кластерной структуры edge не создаёт: пока детекция срабатывает, торговля идёт по уже мёртвым кластерам, а подтверждений, что кластерная структура стабильнее PF, нет ни одного.","3) Отбор/выборка: «торговля только в исторически прибыльных кластерах» — это in-sample отбор прибыльного подмножества на тех же данных, где считалась прибыльность: тот же паттерн автовыбора winner, который проект систематически уличал в переобучении (2.4: winner нестабилен 2/5 сидов; 2.12: 0/11 multi-seed, entry-quality-filter val PF 5.50 → frozen 1.95). Календарная доминантность (2.12) означает, что «прибыльные кластеры» ≈ уже известные прибыльные часы, а вклад ATR-квантиля будет in-sample шумом; фильтр по кластерам режет выборку до десятков сделок (2.2: кандидатские N — десятки), при которых bootstrap CI не даёт нижнюю границу > 1.0 при пороге PF ≥ 1.3."],"falsification":{"test":"Замороженная режимная карта. На строгом обучающем окне (например, 2010–2022), только live-safe источники и зафиксированный контракт хронологии fill (раздел 6), строится кластерный профиль (час × ATR-квантиль) и карта кластер→прибыльность; карта замораживается (SHA-256). Оценка только на нетронутом окне 2023–2026: входы только в кластерах, исторически прибыльных на тренировочном окне, со спредом 0.4–0.8 и проскальзыванием, bootstrap 95% CI. Контроль: тот же режимный фильтр без ATR-компонента (только часовая кластеризация), чтобы отдельно измерить вклад взаимодействия час×ATR.","metric":"PF на замороженном OOS 2023–2026 + нижняя граница bootstrap 95% CI; вторично — прирост над hour-only базилайном (PF_regime − PF_hour_only) с CI.","kill_threshold":"PF ≤ 1.05 или нижняя граница CI ≤ 1.0 на 2023–2026 — идея мертва (ожидание по 2.8/2.12: PF 0.82–0.94). Дополнительно: если PF_regime − PF_hour_only ≤ 0.1 с CI, пересекающим ноль, — амплитудная часть пуста, остаётся известная календарная доминантность.","cost":"3–5 дней","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["novelty/duplicate: механизм «режим объясняется временем+ATR» дословно подтверждён в 2.11, и его торговая эксплуатация провалена: Direction Inside Regime FAIL (2.11), movement-filter заморожен research-only, 0/11 multi-seed, winner-правила time_only (2.12), календарная доминантность (раздел 4). Кластеризация — переупаковка без нового источника прибыли. Это фатально и неустранимо.","stationarity: тезис о квази-стационарности непроверен и противоречится 2.8: перелом 2023 устойчив к расширению обучения, permutation p=1.000; «исторически прибыльные кластеры» — именно умершие в 2023. Принято полностью.","selection: автовыбор прибыльного подмножества воспроизводит паттерн winner-переобучения (2.4: 2/5 сидов; 2.12: 0/11), а малые N (2.2) лишают bootstrap CI силы. Принято с оговоркой: постановка теста это частично лечит, но не спасает."],"defense":"Честно защитить можно только постановку проверки, не идею. Тест не «продаёт» гипотезу: карта замораживается на 2010–2022 (SHA-256), оценка только на нетронутом 2023–2026 с live-safe источниками и контрактом хронологии fill (раздел 6) — отбор прибыльных кластеров вне окна оценки, поэтому смерть кластеров в 2023 тест поймает, а не скроет. Вторичный критерий PF_regime − PF_hour_only с CI, пересекающим ноль, прямо проверяет пустоту ATR-компонента (2.12), не маскируя календарную доминантность. Механизм «кто платит» признаю слабым. Но достоинства теста не создают edge: аргумент 1 фатален, нового источника прибыли нет; ожидаемый исход по 2.8/2.12 (PF 0.82–0.94) — гибель идеи."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Rank stability confidence [кластер 15]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Идей в кластере: Rank stability confidence; Bootstrap agreement meta-signal
- Блок идеи:
```
Название: Rank stability confidence
Суть: Вместо одной модели — ансамбль из K моделей на разных подвыборках признаков. Устойчивость рангового порядка предсказаний (Kendall tau между подвыборками) — мета-сигнал: высокий agreement = торговать, низкий = пропуск. Не выбирает winner-правило, а измеряет надёжность самого факта торговли.
Обходимый тупик: автовыбор winner: ансамбль не выбирает лучшую модель, а агрегирует все; малые выборки: agreement устойчивее абсолютного значения при малом N.
Откуда edge: [гипотеза] если множество моделей с разными подмножествами признаков согласны, что текущий бар — торговый, это свидетельство структурной выраженности сигнала, а не артефакта одной модели.
Теги: 
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Мета-сигнал надёжности/согласия моделей как гейт торговли без выбора winner — протестирован трижды с нулевым/отрицательным результатом: (а) conformal coverage как фильтр сделок по надёжности, (б) multi-seed согласие моделей как критерий устойчивости (leaderboard 0/11), (в) lib_PIC (надёжность внутри модели). Механизм edge тот же: «согласие = сигнал структурности»; меняется только оценщик надёжности (покрытие/сиды/подвыборки).","retrospective_ref":"2.1: «Conformal (α=0.10): покрытие 90%, при жёстком θ эффекта нет (PF 4.506→4.489, сделки 2203→2187, 15 из 16 отфильтрованных прибыльны)» — гейт по надёжности решения не меняет PF; 2.4: «05-07 seed-отчёты — автовыбор winner нестабилен, только предвыбранный A устойчив (PF>2.0 у 4/5 сидов); … Пост-audit: quantile-правило нестабильно по сидам (2/5)» — согласие между моделями не даёт устойчивости; 2.12: «leaderboard 0/11 multi-seed, все winner-правила time_only/movement_plus_time» — 0 из 11 правил пережили согласие по сидам; 4: «Автовыбор winner-правила — систематически переобучается (см. 2.4, 2.12); устойчивость только у предвыбранных frozen-правил» — стабильность даёт фиксация до оценки, а не агрегация."},"vulnerabilities":["Edge нечего измерять: направление в данных отсутствует (2.1 MI direction FAIL, val p=0.229; 2.7, 2.11 — все постановки провалены, «already moved»). Kendall tau между моделями на подвыборках признаков будет мерить согласие о шуме и календаре, а не «структурную выраженность сигнала»: 2.12 показал календарную доминантность (no-ML hour-baseline ≥85.9% ML PF, все winner-правила time_only) — модели согласны именно на календаре, который PF не даёт. Гипотеза «agreement = edge» противоречит главному выводу ретроспективы: согласие моделей существует, но не конвертируется в прибыль.","Мета-гейт ортогонален реальным убийцам edge: вся прибыль проекта умирала на исполнении, а не на отборе — хронология fill (2.12: PF 2.82–3.42→0.82–0.94 после фикса), спред 0.8 (2.12), requote MT4 (2.5). Agreement рангов предсказаний не зависит от того, как ставится лимитка и что происходит внутри бара; если сигнал ломается на fill-хронологии, добавление consensus-слоя не спасёт — он фильтрует поверх уже невалидного сигнала.","Заявленная устойчивость agreement при малом N опровергнута данными проекта: quantile нестабилен по сидам 2/5, медиана 8 сделок (2.4); leaderboard 0/11 multi-seed (2.12); кандидатские N — десятки (2.2). Плюс порог tau — гиперпараметр, который придётся калибровать на данных: это ровно тот автовыбор winner (по порогу), который ретроспектива объявляет переобучающимся (4); фиксация порога до оценки — та же игра, что предвыбор frozen-правила, только с дополнительным слоем свободы."],"falsification":{"test":"Обучить 10–20 моделей XGBoost (сильнейший baseline, 2.8) на разных подвыборках признаков (live-safe, без future-полей) на 2019–2022; на frozen OOS 2023–2026 с корректной хронологией fill (контракт 2.12) и спредом 0.4 вычислить попарный Kendall tau предсказаний и median tau на бар. Гейт: торговать только бары с median tau ≥ порога (перебор 0..1). Метрика: PF гейтированной выборки vs безусловной с bootstrap CI (нижняя граница), Spearman(median tau, R-множитель сделки), контроль календаря — сравнить tau на подвыборках time_only против структурных признаков (2.12: если tau дублирует час-базлайн — идея мертва).","metric":"PF_gated с bootstrap CI (нижняя граница > 1.0) против PF_unconditional на том же OOS; Spearman(median tau, outcome)","kill_threshold":"Идея убита, если: (1) ни при одном пороге tau PF_gated − PF_unconditional ≤ 0.05, либо (2) Spearman(tau, outcome) ≤ 0.05 на ≥100 сделках, либо (3) median tau не различает прибыльные/убыточные бары (убивает вся структура 2.1/2.11: согласие — это календарь и already-moved), либо (4) N сделок после гейта < 30 на 5+ лет при любом пороге.","cost":"3–5 дней (CPU, обучение 10–20 моделей на существующем пайплайне + гейт-анализ и bootstrap CI)","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["novelty: тот же класс мета-гейта надёжности без выбора winner — три нулевых результата (conformal 2.1, multi-seed 2.4/2.12, lib_PIC 2.6); меняется только оценщик","edge: гипотеза «agreement = структурная выраженность» противоречит главному выводу ретроспективы — согласие моделей существует (2.12: календарная доминантность >=85.9%), но не даёт PF; направление отсутствует (2.1, MI direction FAIL val p=0.229)","порог tau калибруется на данных — это автовыбор по порогу, который секция 4 объявляет переобучающимся","устойчивость при малом N опровергнута: quantile 2/5 сидов, медиана 8 сделок (2.4), leaderboard 0/11 (2.12)","ортогональность реальным убийцам: fill-хронология (2.12 PF 2.82-3.42 -> 0.82-0.94), спред 0.8, requote (2.5) — consensus-слой их не чинит"],"defense":"Проверил цитаты в docs/audit/retrospective.md — критика точна, цитаты аутентичны. Признаю поражение по всем пунктам. Механизм edge совпадает с классом уже фальсифицированных мета-гейтов: главный вывод ретроспективы прямо утверждает, что согласие моделей существует, но не конвертируется в прибыль (2.12 — модели согласны на календаре, который PF не даёт). Гипотеза «agreement = сигнал структурности» противоречит этому документированному факту, а не просто не подтверждена. Заявленная устойчивость agreement при малом N также опровергнута данными проекта (2/5 сидов, медиана 8 сделок). Единственный технический нюанс в мою пользу: оценщик иной (ранговая робастность к удалению признаков, а не калибровка или seed-стабильность), и предложенный тест с контролем календаря и контрактом fill был бы корректен и дёшев. Но это не спасает: prior всего класса гейтов нулевой, а калибровка порога tau воспроизводит ровно тот автовыбор winner, который ретроспектива объявляет переобучающимся. Идею снимаю."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Volatility risk premium harvesting [кластер 16]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Идей в кластере: Volatility risk premium harvesting; Carry-momentum interaction on EM FX
- Блок идеи:
```
Название: Volatility risk premium harvesting
Суть: Систематическая продажа OTM strangles на FX-опционах (или их синтетиках через котировки implied vol). Направление не нужно — edge в структурном превышении implied over realized volatility. Частота решений — раз в день/неделю, не на бар.
Обходимый тупик: Информационная граница (MI direction FAIL, 2.1) и внутрибаровая хронология fill (2.12)
Откуда edge: [гипотеза] хедж-фонды и корпораты систематически переплачивают за страховку через options premium; realized vol стабильно ниже implied — это документированный академический эффект
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"Да — это точный дубль уже убитой идеи «Volatility risk premium harvesting» [к19] из предыдущего раунда арбитража (docs/archive/brainstorm-filtered_deep.md, 2026-08-15): вердикт «убита», причина: «Нет данных implied vol / опционов; FX-премия слабая; нестационарность (защищённый variance-swap прокси на OHLC не спас)», автор признал аргумент в раунде 2, защитив лишь тест. В самой ретроспективе (секции 2 и 4) аналога с тем же механизмом edge (премия implied over realized, без направления) нет — ни опционов, ни implied vol, ни премии за риск в проекте не исследовалось, поэтому строгая проверка по секциям 2/4 чиста; но проект уже задокументировал смерть этой идеи в собственном арбитраже.","retrospective_ref":null},"vulnerabilities":["Данные: во всём проекте нет ни одной котировки implied vol или опционной премии — сам edge (IV > RV) неизмерим, невоспроизводим и нeторгуем; доступен только одноисточниковый OHLC, а раздел 7 ретроспективы фиксирует: «одноисточниковый OHLC меняет результаты (см. 2.12, 2.13)». Защищённый в прошлом раунде variance-swap прокси на OHLC — не эквивалент премии и идею не спас.","Исполнение и метрика: короткий OTM strangle — неограниченный хвостовой риск, а задокументированный режимный перелом на границе 2023 («провал 2023–2026 устойчив», 2.8) — это класс событий, съедающих месяцы премии одним шоком; PF + bootstrap CI (lower > 1.0) на хвостовом распределении требует десятилетий истории, которых нет. В контуре нет опционной исполняемости (только market orders MT4/MT5, requote ERROR-138 — 2.5, OPEN_FAILED=22767 — 2.13), а синтетическая репликация через дельта-хедж возвращает внутрибаровую хронологию fill — тупик 2.12, который идея якобы обходит.","Стационарность и происхождение edge: VRP нестационарен по режимам (раздел 7: 2004–2026 неоднородны), в мажорных парах FX премия за последнее десятилетие компрессировалась — «FX-премия слабая» уже зафиксировано вердиктом прошлого раунда; происхождение edge экстернально ([гипотеза], вне данных проекта), тогда как каждый внутренний edge проекта умирал при честной перепроверке; календарная доминантность (2.12: hour-baseline ≥85.9% ML PF) грозит свести любой недельный FX-сигнал к экспири/выходным эффектам, а не к премии."],"falsification":{"test":"Уже сформулирован и исполнен в прошлом раунде: variance-swap прокси на OHLC проекта — короткая синтетическая дисперсия против реализованной за 1W-эквивалент, 10+ лет, 4+ мажорные пары, издержки 2× спред, вход по календарю; результат идею не спас. Полноценный убивающий тест на реальных IV-котировках против RV в проекте не формулируется: опционных данных нет и в контуре их неоткуда взять.","metric":"Чистая годовая доходность продажи дисперсии после издержек, выраженная в PF-эквиваленте на R-кратных, с bootstrap CI; контроль календаря и спреда.","kill_threshold":"PF-эквивалент < 1.3 или CI lower ≤ 1.0; или суммарный net PnL ≤ 0 за 10+ лет; или 99-й перцентиль недельных убытков > 20% годовой собранной премии (хвост доминирует).","cost":"Прокси на OHLC: 1–2 дня (уже исполнен, не спас); реальный тест на IV-котировках: недели+ и вне текущей инфраструктуры (закупка опционных данных).","falsifiable":true},"preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["duplicate_of — точный дубль [к19] (docs/archive/brainstorm-filtered_deep.md, строки 43 и 69), вердикт «убита», автор уже признал аргумент в раунде 2","vulnerabilities[0] — в проекте нет котировок IV/премий, edge (IV>RV) неизмерим; variance-swap прокси на OHLC исполнен и не спас идею","falsification — убивающий тест уже сформулирован и исполнен в прошлом раунде; реальный тест на IV-котировках вне инфраструктуры"],"defense":"Признаю дубль и оба ключевых аргумента без оговорок. Цитата из строки 69 подтверждается дословно: «Нет данных implied vol / опционов; FX-премия слабая; нестационарность; защищал фальсификацию через variance-swap прокси на OHLC» — я уже признал это в раунде 2, защитив только тест. Новых фактов в защиту нет: OHLC-прокси выполнен и не спас идею, реальных IV-котировок в контуре нет и неоткуда взять, хвостовой риск strangle и календарная доминантность (2.12, hour-baseline ≥85.9%) добавляют неисправимые ограничения исполнения. Единственное неустранимое ядро edge (академический факт IV>RV) экстернально и не проверяемо на данных проекта, поэтому спекулятивно по определению. Допускаю также, что «кто платит» (хедж-фонды/корпораты) — слабый механизм, но это уже несущественно на фоне фатального отсутствия данных. Повторное выдвижение идеи без нового источника IV-данных не имеет смысла: фальсификация уже выполнена, вывод «убита» устойчив."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Negative selection — when NOT to trade [кластер 17]

- Статус: досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита»
- Блок идеи:
```
Название: Negative selection — when NOT to trade
Суть: Модель предсказывает не вход, а пропуск: оценивает ожидаемые транзакционные издержки (spread widening, low volatility, session overlap) и фильтрует периоды, где cost-to-edge ratio неблагоприятен. Edge — не в прибыльных сделках, а в избежании убыточных.
Обходимый тупик: Малые выборки и хрупкая годовая стабильность (2.2, 2.4, 2.12)
Откуда edge: [гипотеза] транзакционные издержки кластеризуются во времени; платят маркет-мейкеры, которые забирают spread в моменты, когда трейдер вынужден войти
Теги: радикальная, похоже на: календарная доминантность
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":"календарная доминантность / time-based negative selection (session-фильтры: NY-exclusion, hour-baseline, frequency-skip)","retrospective_ref":"секция 4: «Календарная доминантность (см. 2.12) — no-ML hour-baseline догоняет ML-PF, формальные winner-правила оказываются time_only; некалендарный сигнал не выделен»; секция 2.12: «no-ML hour-baseline ≥85.9% ML PF (медиана 0.98) — календарная доминантность; leaderboard 0/11 multi-seed, все winner-правила time_only/movement_plus_time»; секция 2.4: «pf-uplift-discovery (Нью-Йорк исключить +12.1 PF, выход bar=12 +5.55); frequency REJECT (val 0.22)»; секция 2.6: «frequency top_k17% тест PF 13.12... работает только старый календарно-частотный контур»"}, "vulnerabilities":["ДАННЫЕ: в проекте нет реального временного ряда спреда/издержек — spread моделируется фиксированным каноническим параметром (2.12: «spread 0.2→PF 3.27, 0.4→2.72, 0.8→1.32»), а не признаком. «Оценивать ожидаемые транзакционные издержки» не на чем обучать: спред не кластеризуется в данных, он константа бэктеста; гипотеза о маркет-мейкерах («забирают spread в моменты, когда трейдер вынужден войти») ничем не подтверждена и непроверяема на имеющемся OHLC.","САМОПОРАЖЕНИЕ МЕХАНИКИ: negative-selection фильтр сокращает число сделок, то есть УСИЛИВАЕТ тупик малых выборок и хрупкой годовой стабильности (2.2, 2.4), который идея заявляет обойти. Механизм уже проверен и не прошёл барьер: time_only/календарные правила доминировали, но не прошли multi-seed гейты (2.12 leaderboard 0/11), а NY-исключение было частью отозванной quantile-цепочки (2.4 post-audit: «quantile-правило нестабильно по сидам (2/5)»). Фильтр — не решение малых N, а его усугубление.","СТАЦИОНАРНОСТЬ/РЕЖИМ: время-зависимые фильтры не устойчивы к режимному перелому 2023 (2.8: «провал 2023–2026 устойчив к расширению обучения»); паттерны сессий/оверлапа сдвигаются между режимами, а обучение фильтра на pre-2023 не гарантирует переноса. Кроме того, в 2.5 показано, что реальный блокер исполнения — requote ERROR-138, а не спред-режимы; «cost-to-edge» в живом контуре упирается в исполнение, которое уже режектит входы независимо от фильтра."],"falsification":{"test":"Взять замороженный стабильный базис (движение-фильтр 2.11 или предвыбранный baseline A 2.4) и поверх него применить negative-selection фильтр на прокси издержек (session overlap + volatility proxy + stress spread 0.4/0.8) на строгом OOS 2023-2026 с честной внутрибаровой хронологией fill (контракт 2.12/2.5). Сравнить: (1) PF фильтрованной системы против unfiltered baseline, (2) PF против no-ML hour-baseline, (3) bootstrap CI и N сделок.","metric":"Net PF на OOS с каноническим спредом + bootstrap CI нижняя граница (BS_p05) + число сделок","kill_threshold":"BS_p05 ≤ 1.0; ИЛИ PF_filt ≤ PF_unfilt (нет uplift от фильтра); ИЛИ PF_filt ≤ 1.05 × PF_hour_baseline (календарь объясняет весь эффект — ровно исход 2.12); ИЛИ N_filt < 100 сделок (механизм сам себя обнуляет)","cost":"3-5 дней","falsifiable":true}, "preliminary_verdict":"убита"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["novelty: дубликат календарной доминантности (2.12: hour-baseline ≥85.9% ML PF, leaderboard 0/11; 2.4: NY +12.1, frequency REJECT)","data: спред — константа бэктеста, cost-механизм необучаем, маркет-мейкерная гипотеза непроверяема","самопоражение: фильтр сокращает N, усиливая тупик малых выборок"],"defense":"Проверил цитаты в retrospective.md — они точны (2.12, 2.4; спред как константа 0.2/0.4/0.8). Novelty-аргумент фатален: мой механизм есть уже задокументированная календарная доминантность — session-фильтры проверены и не прошли multi-seed гейты; собственный тег «похоже на» это подтверждает. Data-аргумент фатален: спред — константа, не признак; обучать «оценку издержек» не на чем; история про маркет-мейкеров непроверяема на OHLC; остаются прокси (volatility/session), которые и породили календарную доминантность — отличительного cost-сигнала нет. Самопоражение признаю: фильтр сокращает N и усугубляет тупик малых выборок; порог N_filt<100, вероятно, сработает. Режимный довод (2.8) корректен, но бьёт по всем идеям проекта. Тест фальсификации честен, но вероятный исход — ровно 2.12: PF_filt ≈ hour-baseline. Идея не проходит."}
```

### Раунд 3 — итог критика
Пропущен — досрочный выход: автор признал фатальный аргумент при предварительном вердикте «убита». Вердикт: убита.

## Execution venue asymmetry [кластер 18]

- Статус: полный спор (3 раунда)
- Блок идеи:
```
Название: Execution venue asymmetry
Суть: Использовать разницу в исполнении между лимитными и рыночными ордерами на разных сессиях: размещать лимитки на одной сессии (Asian, узкий spread для определённых пар), а исполнение получить на другой (London/NY, расширение). Edge в микроструктуре, не в прогнозе цены.
Обходимый тупик: Внутрибаровая хронология fill (2.6, 2.12) и single-source OHLC (2.12, 2.13)
Откуда edge: [гипотеза] асинхронность ликвидности между сессиями создаёт предсказуемый паттерн fill quality; платят трейдеры, которые входят рыночными ордерами в illiquid hours
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":null,"retrospective_ref":null},"vulnerabilities":["Источник edge ненаблюдаем: у проекта нет данных для проверки микроструктурной гипотезы — нет тиков, нет спреда по сессиям, нет книги ордеров. Ретроспектива прямо фиксирует, что одноисточниковый OHLC меняет результаты (2.12: '07-29 current-history — PnL 4429.78R→4065.03R, зависит от источника OHLC'; 2.13), а телеметрия спреда только планируется (раздел 6: 'Подход: режимная диагностика на OHLC и телеметрии спреда'). Сам автор помечает источник edge как '[гипотеза]' — проверять нечем; утверждение об узком Asian-спреде 'для определённых пар' непроверяемо текущими данными, а кросс-инструментально в проекте выжили только XAGUSD/USDCHF (2.4: 'cross-instrument — только XAGUSD/USDCHF').","Идея построена на тупике, который заявлен как обходимый: механизм edge — это и есть внутрибаровая хронология fill (лимитка сессии A, исполнение в сессии B). 2.12: 'после фикса хронологии fill PnL → −530.51, PF 2.82–3.42→0.82–0.94, edge исчез, locked-test вывод недействителен; mt4-fill-chronology — MT4 ставил лимитки иначе (OPEN_FAILED=404)'; раздел 6: 'Внутрибаровая хронология fill. Известно: от неё зависит весь edge; Python-симулятор и MT4 расходятся по порядку событий'. Пока нет контракта хронологии, любой симуляторный PF по этой идее неотличим от артефакта — заявление 'обходим 2.12' внутренне противоречиво: edge, если он есть, живёт целиком внутри неисправленного слоя, и именно на нём проект уже потерял единственного формально прошедшего locked-test кандидата (раздел 4: 'Фиксированный fractal0-кандидат — edge зависел от хронологии fill; после фикса PF < 1').","Смежная территория уже отвергнута, а исполнение-зависимые результаты не переносятся в реальный MT4: (a) 2.13: '08-01 Execution Hygiene — OPEN_FAILED=22767, fill rate↔PF Spearman −0.46, гипотеза «плохой fill брокера» отвергнута' — филл-качественная гипотеза как объяснение PF уже проверена и отвергнута, причём корреляция fill rate↔PF отрицательная, что скорее указывает на селекцию, а не на эксплуатируемый паттерн; (b) 2.5: '8/87 входов не открылись (requote ERROR-138); блокер — исполнение в MT4 (requote), нужна retry-механика' — решающая проверка идеи требует живого исполнения, которое сейчас заблокировано нерешённым багом; (c) 2.12: 'no-ML hour-baseline ≥85.9% ML PF (медиана 0.98) — календарная доминантность; все winner-правила time_only' — сессионный edge это ровно та категория, которую календарный baseline уже доминирует, и предыдущий сессионный результат (2.4: 'Нью-Йорк исключить +12.1 PF') не пережил перепроверку."],"falsification":{"test":"Решающий тест — живой пробник исполнения на MT5-демо (контур из 2.13 рабочий, там же запланирован 'trade-count/entry-mechanics probe'), независимый от симуляторной хронологии: 4–8 недель в Asian-сессию выставлять resting лимитки на выбранных парах, фиксировать fill rate, цену fill и цену последующего лондонского открытия; контроль — рыночные входы в те же сессии. Историческая версия на исправленном по хронологии OHLC (1–2 дня) — только вспомогательная и не решающая: без контракта хронологии fill (2.12) положительный симуляторный PF неотличим от артефакта, поэтому убивает идею только живое исполнение.","metric":"PF на множестве реальных demo-fill (N≥100) с bootstrap CI; дополнительные метрики — fill rate и adverse selection (медианная цена fill против цены следующего открытия, после спреда и комиссии).","kill_threshold":"PF < 1.3 при нижней границе bootstrap CI ≤ 1.0 на ≥100 реальных fill, ИЛИ fill rate < 60% (стратегия операционально мертва), ИЛИ adverse selection съедает >50% спредового расширения Asian→London (медианное преимущество fill над рыночным входом ≤ 0 pips после издержек).","cost":"недели: 4–8 недель демо-данных плюс часы на анализ; историческая версия — 1–2 дня, но не является решающей; весь тест заблокирован до решения retry-механики requote (2.5)","falsifiable":true},"preliminary_verdict":"условно"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["аргумент 2 — фатален для формулировки: механизм edge (resting-лимитка и время её fill) и есть внутрибаровая хронология, от которой зависел весь edge (2.12); «обходим 2.12» — неверно","аргумент 1 (частично): сессионный спред исторически непроверяем текущими данными","аргумент 3c (частично): риск календарной доминантности реален"],"defense":"Принимаю аргумент 2 без оговорок: моя гипотеза утверждает ровно тот механизм, который 2.12 показал артефактным — fill-хронологию лимиток. Заявлять «обходим 2.12» было ошибкой; честная формулировка — не обход, а перенос проверки на живой демо-fill, где симуляторная хронология не участвует. По аргументу 1: ненаблюдаемость историческая, но тест измеряет спред и fill напрямую в live — эксперимент выполним без тиков. По 3: (a) Execution Hygiene отверг «плохой fill брокера» в контексте батч-политики, а не сессионной асимметрии ликвидности — смежно, но не то же; (b) requote уже учтён в cost как блокер. Риск календарной доминантности (3c) — реальный, поэтому контроль именно рыночные входы в те же сессии: равный PF контроля = календарь, а не venue. Идея выживает только как дешёвый live-пробник, не как исторический edge."}
```

### Раунд 3 — итог критика
```json
{"name": "Execution venue asymmetry", "pros": ["Автор без оговорок признал аргумент 2 и отозвал формулировку «обходим 2.12» — научная честность, но не довод в пользу идеи", "Аргумент 3a отработан по существу: сессионная асимметрия ликвидности действительно не тождественна отвергнутому «плохому fill брокера» (Execution Hygiene — про батч-политику), это разные механизмы", "Автор свёл идею к дешёвому live-пробнику с прямым измерением спреда и fill — как эксперимент он выполним без исторических тиков, но это уже другая, пониженная сущность"], "cons": ["Признан фатальный дефект формулировки: механизм edge (resting-лимитка и время её fill) — ровно та внутрибаровая хронология, которую 2.12 показал артефактной; ядро идеи совпадает с признанным артефактом", "Историческая проверка невозможна: сессионный спред непроверяем текущими данными (аргумент 1, частично признан) — как исторический edge идея неверифицируема", "Риск календарной доминантности час-базлайна признан реальным (3c); контроль рыночными входами реален только в будущем live-эксперименте, ретроспективно не снимает проблему", "Контр-данные смежной территории не опровергнуты: fill↔PF Spearman −0.46 (2.13) — премиса «лучший fill = edge» эмпирически уже получала холодный душ", "Переформулировка «выживает как live-пробник» — это не защита исходной идеи, а её замена: исходное утверждение об историческом edge автором оставлено"], "novelty": {"duplicate_of": null, "retrospective_ref": null}, "vulnerabilities": ["Механизм edge совпадает с признанным артефактом (2.12): fill-хронология лимиток — это не обходимый тупик, а ядро идеи", "Ненаблюдаемость сессионного спреда в исторических данных (2.12, 2.13, single-source OHLC) — edge ретроспективно не установим", "Смежная отвергнутая территория остаётся: Execution Hygiene (2.13), requote (2.5); различие механизмов признано, но не добавляет данных в пользу edge", "Календарная доминантность (2.12) не снята — сессионный эффект может быть артефактом час-базлайна"], "falsification": {"statement": "Если в живом/демо-исполнении систематическая разница fill quality (спред, время и доля исполнения лимиток) между сессиями для выбранных пар отсутствует или не воспроизводится — идея опровергнута", "status": "не изменена", "note": "Защита новых данных не дала; историческая версия фальсификации остаётся непроверяемой текущими данными (аргумент 1)"}, "potential": 1, "potential_why": "По механизму «кто платит» жертвы (рыночники в illiquid hours) существуют, но признанный артефакт механизма, непроверяемость спреда и контр-данные 2.13 не дают оснований для устойчивого edge.", "verdict": "убита", "rationale": "Признанный фатальный дефект механизма (fill-хронология = артефакт 2.12) убивает исходную идею по существу, а сведение к live-пробнику — это понижение статуса, а не защита, поэтому предварительный «условно» меняется на «убита»."}
```

## Gamma hedging flow front-running [кластер 19]

- Статус: полный спор (3 раунда)
- Блок идеи:
```
Название: Gamma hedging flow front-running
Суть: Опционные дилеры обязаны хеджировать gamma exposure у страйков с высокой open interest. Это создаёт предсказуемые pinning и acceleration эффекты около крупных страйков на еженедельных/ежедневных экспирациях. Сигнал из options open interest, не из OHLC.
Обходимый тупик: Направленческий сигнал (2.7, 2.11) и low R² (2.1)
Откуда edge: [гипотеза] дилеры механически хеджируют gamma, создавая предсказуемые потоки; платят direction-трейдеры, которые входят в сторону dealer hedging flow около экспираций
Теги: радикальная
```

### Раунд 1 — атака критика
```json
{"novelty":{"duplicate_of":null,"retrospective_ref":null},"vulnerabilities":["Данные недоступны и, вероятно, несуществующий источник сигнала: весь контур проекта — OHLC (MT4/MT5, Nero.csv); опционных данных (open interest по страйкам) в проекте нет нигде (проверено поиском по docs/, wiki/, *.py — совпадений с options/gamma/pinning/open interest ноль, все найденные 'gamma'/'expiry' — Focal Loss и срок действия ордеров). Forex-опционы — внебиржевой (OTC) рынок: публичного исторического OI по страйкам нет в принципе (только агрегаты DTCC без страйковой разбивки или волатильные поверхности, из которых OI не восстанавливается). Ретроспектива фиксирует, что даже смена источника OHLC меняет результаты (2.12, 2.13); здесь же требуется новый внешний провайдер с историей в 5–20 лет, которой нет — bootstrap CI с нижней границей >1.0 при десятках независимых экспираций в год не строится на 1–2 годах данных. Механизм входа-в-сторону-дилерского-потока нереализуем без знания дилерской позиции, а она для OTC-опционов ненаблюдаема розничным участником","Идея — направленческий сигнал, который ретроспектива структурно закрыла на имеющихся данных, а необходимое условие механизма даже не проверено: MI-аудит (2.1) даёт MI(direction) 0.003–0.004 бит против amplitude 0.010–0.022, вердикт секции 4 — 'в признаках нет информации о знаке'. Механизм gamma-hedging flow утверждает, что потоки дилеров предсказуемо двигают цену в окне экспирации — но тогда направленное движение обязано быть видимо в OHLC внутри этого окна (необходимое условие), и это проверяемо уже сегодня без опционных данных. Дополнительно: календарная доминантность (2.12) — no-ML hour-baseline догоняет ML-PF, поэтому любой «эффект экспирации», привязанный к времени, обязан быть сравнен с hour-baseline, иначе attribution 'это gamma flow' ложна; а режимный перелом 2023 (2.8) означает неоднородность микроструктуры (глубина опционного рынка, поведение дилеров) на 20-летнем бэктесте","Размер выборки и происхождение edge: ~52 еженедельные экспирации в год дают десятки независимых событий — хронический провальный режим проекта (2.2, 2.4: кандидатские N — десятки, bootstrap CI широкие, годовая стабильность хрупкая); для различения PF 1.3 от 1.0 с нижней границей CI >1.0 нужны сотни независимых сделок в однородном режиме, которых нет. Происхождение edge помечено в самой идее как '[гипотеза]' — а 6 месяцев проекта систематически показывали, что кажущийся edge на валидации есть артефакт постановки (2.5 future-derived признаки, 2.12 хронология fill: PF 2.82–3.42 → 0.82–0.94 после фикса). Даже при наличии OI-данных без контракта внутрибаровой хронологии fill (2.12) и live-safe пересборки (2.5) история повторится — риск не нулевой, а предопределённый"],"falsification":{"test":"Пробник направленной предсказуемости в окнах экспирации на существующих OHLC (опционные данные НЕ нужны). Использовать известное календарное расписание FX-опционов (еженедельные экспирации: среда 10:00 NY; плюс пятничные) на всех инструментах проекта. Для каждого окна −6..−0 баров до экспирации: (a) обучить направленный классификатор на тех же признаках, что в 2.7/2.8, + время/календарь, канонический сплит val 2019–2022 / eval 2023–2026, измерить OOS AUC внутри окон против той же модели вне окон; (b) механический PF входа в окне экспирации с контрактом хронологии fill (2.12) и нетто спреда 0.4–0.8, сравнить с hour-baseline (2.12). Логика убийства: если направленное движение в самом окне, где оно заявлено, не детектируется сильнее, чем вне окна, — необходимое условие механизма (потоки дилеров двигают цену предсказуемо) не выполняется, и никакой OI-сигнал не даст PF ≥ 1.3 нетто. Дополнительный быстрый gate (1 день): проверить публичную доступность исторического OI по OTC-страйкам FX на ≥5 лет — отсутствие источника убивает идею на входе как нереализуемую","metric":"(a) разность OOS AUC: AUC(внутри окон экспирации) − AUC(вне окон) с bootstrap CI; (b) PF механической стратегии в окнах экспирации, нетто спреда, против hour-baseline PF","kill_threshold":"Идея убита, если AUC(внутри окон) − AUC(вне окон) < 0.02 при CI, пересекающем 0.5, ИЛИ механический PF в окнах < 1.3 с bootstrap CI нижней границей ≤ 1.0, ИЛИ прирост PF над hour-baseline ≤ 0.05. Достаточно любого из трёх; все три — необходимые условия механизма","cost":"3–5 дней (только OHLC, существующая инфраструктура классификации и PF-оценки) + 1 день на data-feasibility gate по OI","falsifiable":true},"preliminary_verdict":"условно"}
```

### Раунд 2 — ответ автора
```json
{"concession":true,"accepted_arguments":["Данные недоступны: публичного исторического OTC-опционного OI по страйкам нет, вход в сторону дилерского потока ненаблюдаем розничным участником — фатально для исходной формулировки (сигнал из open interest)"],"defense":"Принимаю фатальный аргумент: исходная формулировка мертва, т.к. её сигнал (OI по страйкам) нереализуем на доступных данных. Остаюсь по остальным пунктам: 1) MI-аудит (2.1, секция 4) измерял признаки без условия на время/окно. Вердикт «нет информации о знаке» относится к тому набору признаков; критик сам добавляет время/календарь и окна — признаёт, что условная постановка не проверена. Прецедент сильный (2.7, 2.11), но закрывает безусловную версию, не эту. 2) Необходимое условие (эффект обязан быть виден в OHLC внутри окна) верно и превращает идею в фальсифицируемый пробник на существующих данных — что и есть его тест. Против не спорю. 3) N: еженедельных экспираций — десятки, но FX-опционы имеют ежедневный cut 10:00 NY — сотни событий в год, это смягчает 2.2/2.4 (сотни независимых входов на 2004–2026). Режимный перелом (2.8) и hour-baseline (2.12) уже вшиты в его дизайн сплита и сравнения. 4) «Кто платит» — слабый механизм, принимаю как [гипотеза], как и помечено в идее. Вывод: concession по сигналу из OI; защищаемый остаток — его же OHLC-пробник предсказуемости в окнах экспирации, который он сам предложил проверить."}
```

### Раунд 3 — итог критика
```json
{"name":"Gamma hedging flow front-running","pros":["Механизм реален в теории: дилерское гамма-хеджирование и пиннинг у крупных страйков документированы в литературе, гипотеза научно обоснована как гипотеза","Необходимое условие (предсказуемость движения в окне экспирации) проверяемо на существующих OHLC-данных без внешних опционных данных — дешёвый фальсифицируемый пробник","Защита дала новое данное: FX-опционы имеют ежедневный cut 10:00 NY, что даёт сотни событий в год вместо десятков и смягчает пункт о малом N (2.2/2.4)","Честное признание фатального аргумента без подмены тезиса: автор не имитирует защиту"],"cons":["Ядро идеи — сигнал из open interest по страйкам — нереализуемо: публичного исторического OTC-овского OI нет, дилерская позиция ненаблюдаема розничным участником; аргумент признан автором (concession=true)","Защищаемый остаток (OHLC-пробник предсказуемости в окнах) — это редуцированная другая гипотеза (условный по времени направленческий сигнал), а не исходная идея; правила не позволяют засчитать улучшение гипотезы за автора","Механизм «кто платит» требует знать направление дилерского потока, а оно ненаблюдаемо без OI: даже положительный пробник не конвертируется в реализуемый сигнал — edge_source недостижим","Направленческий сигнал закрыт ретроспективой (2.1 MI direction FAIL); условная версия не измерялась, а прохождение необходимого условия не есть достаточное условие edge; календарная доминантность (2.12) не проверена против hour-baseline"],"novelty":{"duplicate_of":null,"retrospective_ref":"Идея является направленческим сигналом, закрытым ретроспективой MI direction FAIL (2.1); условная по времени/окну версия не измерялась, но это не спасает исходную формулировку с сигналом из OI, т.к. сам сигнал недоступен"},"vulnerabilities":["Сигнал из OI недоступен на любых имеющихся данных — исходная формулировка нереализуема в принципе","Даже при положительном результате OHLC-пробника направление дилерского потока остаётся ненаблюдаемым — сигнал строить не из чего, пробник проверяет необходимое, но не достаточное условие","Пробник требует контроля календарной доминантности (2.12) сравнением с hour-baseline, иначе положительный результат может быть артефактом календаря","Условность по окну/времени не была измерена в MI-аудите (2.1), поэтому результат пробника не подкреплён ретроспективой"],"falsification":{"statement":"В окнах экспирации (ежедневный cut 10:00 NY) движение цены предсказуемо по календарю сверх hour-baseline","test":"Сравнение метрик предсказуемости в окнах экспирации с hour-baseline и проверка календарной доминантности (2.12) на имеющихся OHLC-данных","data_required":"Только существующие OHLC-данные проекта; внешние опционные данные не требуются","data_added_by_defense":"Ежедневный cut 10:00 NY даёт сотни событий в год, снимая проблему N в десятки","accept_if":"Пробник устойчиво превосходит hour-baseline и не объясняется обычным календарём","reject_if":"Предсказуемость в окнах не отличается от hour-baseline или полностью объясняется календарной доминантностью"},"potential":1,"potential_why":"Механизм «кто платит» требует знания направления дилерского потока, которое ненаблюдаемо без OI — edge недостижим розничным участником, и автор сам признал механизм слабой гипотезой","verdict":"убита","rationale":"Признанный аргумент фатален по существу: единственный сигнал идеи (OI по страйкам) нереализуем на доступных данных, а защищаемый остаток — это другая, редуцированная гипотеза (OHLC-пробник), которую правила не позволяют засчитать за исходную идею"}
```
