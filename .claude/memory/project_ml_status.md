---
name: project_ml_status
description: Статус ML и утверждённый план замены таргета predict на up/dn
type: project
---

Все 4 модели провалились (PF=0.728 < 1.0). Причина — шумный таргет predict с переменным горизонтом.

**Утверждённый план (2026-03-18):** Замена predict на direction-independent up/dn таргеты.
- up_N = max(High) - F[f].P, dn_N = F[f].P - min(Low) за фиксированные горизонты N={12,24,48} баров H1
- Entry price = F[f].P (цена фрактала: High для пиков, Low для впадин)
- Вычисление в MQL4: Up[3]/Dn[3] добавляются в структуру PICS (head_PIC.mqh)
- Накопление инкрементально в LEVELS_FIND_AROUND() для ВСЕХ фракталов без исключения
- Запись: расширяем 11 полей фрактала до 17 (добавляем Up[3]+Dn[3]) в NERO_CSV_CREATE
- Сортировка фракталов по времени — в Python (как раньше)
- Python label_signals.py: извлекает таргет нового фрактала из более поздних строк по fractal_time
- ATR_ratio и hour/dow — позднее, отдельно

**Критерий успеха:** PF > 1.5 при ratio_min=1.5, даже если торгуются только 20-30% сигналов.
