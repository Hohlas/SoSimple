## agetns promts

```md
Напиши план по этой теме.
Используй скилл superpowers/writing-plans.
Если тебе недостаточно данных для надёжного плана, явно укажи неизвестное и вопросы для уточнения.

Методология проекта обязательна:
  - начни с `docs/methodology/README.md`;
  - выбери только те разделы `docs/methodology`, которые относятся к теме;
  - для каждого этапа плана укажи применимую методику, обязательные проверки и критерий завершения;
  - если подходящего раздела методологии нет, прямо укажи это и предложи обоснованный порядок действий.
  - добавь к плану контекст, необходимый другому ии агенту, который имеет доступ к этому repo, но не имеет истории нашего диалога. Таким образом, план должен быть самодостаточен для холодного старта.
```

```md
Выполни план
Если доступны подагенты и задачи независимы, используй `superpowers:subagent-driven-development`; иначе используй `superpowers:executing-plans`.
Выполняй задачи строго по порядку.
Запускай проверки, требуемые планом и правилами проекта.
В финальном ответе кратко укажи: что сделано, какие проверки выполнены, какие риски остались.
```

```md
Проверь его полностью.
Точечно изучи контекст проекта, необходимый для погружения в тему данного документа.
Точечно проверь только реально связанные с ним артефакты (ссылки из документа, упомянутые файлы, ближайшие README, планы, отчёты, методики или код), без которых нельзя подтвердить конкретное утверждение. 
Для поиска используй graphify и knowledge-rag.

Проведи аудит по фактам, а не по впечатлениям. Не пиши общие замечания без доказательств.

Для каждого утверждения, вывода или решения проверь:
  - фактические ошибки и противоречия;
  - неподтверждённые утверждения;
  - несоответствия методологии docs/methodology;
  - что стоит добавить, убрать или уточнить.

Для каждого замечания укажи:
  - важность: критично / важно / улучшение / вопрос;
  - место: файл, раздел или строка, если возможно;
  - суть проблемы;
  - доказательство: файл, строка, команда, документ или явное указание, что это гипотеза;
  - почему это важно;
  - рекомендуемое исправление.

Полностью очисти файл `docs/superpowers/audit.md`, не читай его содержимое. 
Сохрани аудит в этот файл. 
```

```md
Это аудит твоей работы - docs/superpowers/audit.md. Он не является источником истины. Прочитай его полностью. 
Если есть неясные замечания, сначала явно укажи, что именно непонятно, и не исправляй их без уточнения.
Проверь каждое замечание критически по исходным файлам проекта, релевантной методологии и применимым проверкам.

Для каждого пункта аудита:
  - определи статус: принято / частично принято / отклонено / требует уточнения;
  - если принято — внеси минимально необходимые исправления;
  - если частично принято — исправь подтверждённую часть и кратко объясни, что не принято;
  - если отклонено — кратко докажи почему, со ссылкой на артифакт (файл, строку, команду, ...);
  - если требует уточнения — явно укажи, чего не хватает для решения.

Не делай посторонний рефакторинг и не исправляй то, что не относится к аудиту.

После исправлений запусти применимые проверки. 
В финальном ответе перечисли:
  - что исправлено;
  - что отклонено и почему;
  - какие проверки выполнены;
  - какие риски остались.
```

```md
объясни простым языком содержимое этого документа
```
